# Audit: Full Codebase Audit

> **Date:** 2026-04-18
> **Trigger:** Periodic full audit — ~30 commits / ~3,600 net LOC since audit #11 (2026-04-17). Major changes: recipes.ts route split into 4 modules, MealPlanDay type-dep inversion, Coach Pro hardening (notebook dedup, warm-up cache key, pure-fn extraction), MiniSearch filter-chain consolidation + community mealType classification, seed-recipes prod guard + parallel pipeline, SkeletonBox shared shimmer timer, eval framework hardening, recipe-wizard a11y polish, cleanup script hardening, recipe-catalog save + URL import premium gating.
> **Domains:** security, performance, data-integrity, architecture, code-quality, camera, ai-llm, nutrition
> **Baseline:** 3846 tests passing (265 files) | 0 type errors | 0 lint errors (3 warnings)
> **Raw agent findings:** 91 across 8 domains (9 security, 15 database, 12 AI/LLM, 9 RN UI/UX, 16 architecture, 12 testing, 10 nutrition, 8 camera) → 83 after dedup
> **Post-fix:** 3857 tests passing (265 files) | 0 type errors | 0 lint errors (3 warnings)

## Findings

Each finding has a lifecycle: `open` → `fixing` → `verified` or `deferred` or `false-positive`.

### Critical

| ID  | Finding | Domain | Agent | File(s) | Status | Verification |
| --- | ------- | ------ | ----- | ------- | ------ | ------------ |
| —   | —       | —      | —     | —       | —      | —            |

### High

| ID  | Finding                                                                                                                                                                                                                                                                                                                    | Domain                       | Agent                  | File(s)                                                                                      | Status   | Verification                                                                                                                                                                                                                                                                                                                                                            |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ---------------------- | -------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | `POST /api/meal-plan/recipes/generate` checks daily quota but never inserts a `recipeGenerationLog` row — quota check always passes → unlimited AI burn for a premium user                                                                                                                                                 | security, data-integrity     | Architecture           | `server/routes/recipe-generate.ts:37-49,62-65`                                               | verified | New `logRecipeGenerationWithLimitCheck` (atomic count+insert tx, recipeId nullable); route calls it after AI content returns; 9/9 recipe-generate tests incl. new H1 log-called + 429 TOCTOU tests pass                                                                                                                                                                 |
| H2  | Same endpoint passes `userProfile: null` — ignores user allergens/diet; sibling `POST /api/recipes/generate` fetches the profile. Allergen safety gap for AI recipe generation                                                                                                                                             | security, data-integrity     | Architecture           | `server/routes/recipe-generate.ts:64`                                                        | verified | Route now calls `storage.getUserProfile(req.userId)` before `generateRecipeContent`; new H2 regression test asserts `userProfile` threaded to generator; 9/9 tests pass                                                                                                                                                                                                 |
| H3  | `getUnifiedRecipes` hardcodes "community recipes have no mealTypes column" — but commit `945df21` added one. `GET /api/recipes/browse?mealType=breakfast` returns dinners from community pool                                                                                                                              | data-integrity               | Database, Architecture | `server/storage/meal-plans.ts:420-429`                                                       | verified | Added symmetric `communityConditions.push(...)` with `@>`/empty-OR-null branch; new regression test asserts community breakfast kept + community dinner excluded; 88/88 meal-plans tests pass                                                                                                                                                                           |
| H4  | Coach response cache key missing `isCoachPro` — a Pro user's first message reads a cached non-Pro answer, bypassing tool calls, notebook injection, coach_blocks                                                                                                                                                           | data-integrity, security     | AI/LLM                 | `server/services/coach-pro-chat.ts:63-68, 260-285`                                           | verified | `isCacheable` now `&& !isCoachPro`; `hashCoachCacheKey` signature adds `isCoachPro` + `dayBucket`; new regression test asserts Pro/free produce different hashes; 36/36 tests pass                                                                                                                                                                                      |
| H5  | Coach cache key omits `goals`, `todayIntake`, `weightTrend`, time — "Is my diet okay today?" serves yesterday's answer for 7 days (default TTL)                                                                                                                                                                            | data-integrity               | AI/LLM                 | `server/services/coach-pro-chat.ts:63-68`                                                    | verified | Cache key now includes UTC `dayBucket` (`YYYY-MM-DD`) and `COACH_CACHE_VERSION` tag — stale entries cache-miss at midnight UTC; new regression test; 36/36 tests pass                                                                                                                                                                                                   |
| H6  | Coach Pro tool-call loop breaks on budget without yielding closing text — user sees truncated / empty reply when the model was about to make tool calls                                                                                                                                                                    | data-integrity, code-quality | AI/LLM                 | `server/services/nutrition-coach.ts:336-340`                                                 | verified | On overshoot the generator now yields a short "tool-call budget exhausted" wrap-up appended to `fullResponse` (read on line 389 safety check + persisted). 17/17 nutrition-coach tests pass; re-read confirms the yield happens before `break`                                                                                                                          |
| H7  | `GET /api/meal-plan/catalog/search` + `/:id` lack premium gate — Spoonacular quota drained by free-tier users (sibling POST endpoints correctly gated by commit `b663764`)                                                                                                                                                 | security                     | Security               | `server/routes/recipe-catalog.ts:66-106, 108-147`                                            | verified | Both endpoints now call `checkPremiumFeature(req, res, "catalogSave", "Recipe catalog")` before Spoonacular; new 403 regression tests assert the service is never called for free users; 68/68 recipes tests pass                                                                                                                                                       |
| H8  | `batchUpdateMealTypes` / `batchUpdateCommunityMealTypes`: (a) runs N serial UPDATEs inside one tx, (b) doesn't call `addToIndex` → MiniSearch filter returns stale pre-backfill results until restart                                                                                                                      | performance, data-integrity  | Database, Architecture | `server/storage/meal-plans.ts:1206-1223`, `server/storage/community.ts:287-304`              | verified | Both functions now do a single `UPDATE … FROM (VALUES …)` round-trip and refresh `addToIndex` for each doc via `getDocumentStore()`; new batchUpdateMealTypes regression test asserts both rows take new mealTypes; 111/111 storage tests pass                                                                                                                          |
| H9  | `coach-warm-up` session store is instantiated in `services/` (should be `storage/`) AND reads `warmUpStore._internals.store.get(key)` in prod code — `_internals` is docstring-labeled "never import from production"                                                                                                      | architecture                 | Architecture           | `server/services/coach-warm-up.ts:11,43-48,100`                                              | verified | `consumeWarmUp` now uses the public `warmUpStore.get(key)` API; grep for `warmUpStore._internals` in `server/` returns 0 non-comment hits; 48/48 coach-pro-chat + coach-context tests pass. Store-layer relocation deferred to architecture-followups todo as it requires broader refactor.                                                                             |
| H10 | `community_recipes` has no nutrition columns; `communityToSearchable` hardcodes `caloriesPerServing/proteinPerServing/…` = `null`. Combined with H10 (2026-04-17) null-exclusion, any `maxCalories`/`minProtein` filter silently drops the entire community pool (including 25 seed recipes)                               | data-integrity               | Nutrition              | `server/lib/search-index.ts:124-127`, `shared/schema.ts:480-542`                             | verified | Added source-aware `numericPassThrough` — community recipes with `null` nutrition now pass through macro filters (unknown > excluded); personal recipes with null still excluded (user data is authoritative); 3 affected tests updated + 1 new personal-null regression added; 40/40 recipe-search tests pass. Schema + backfill deferred to nutrition-followups todo. |
| H11 | `createChatMessageWithLimitCheck` accepts `userId` + `conversationId` but never joins on ownership inside the tx; insert + conversation UPDATE happen without verifying `conversations.userId = userId`. Route callers pre-check, but storage contract is an IDOR footgun                                                  | security, data-integrity     | Database               | `server/storage/chat.ts:167-334`                                                             | verified | Added ownership SELECT inside the tx (after advisory-lock, before any quota query or insert) — returns `null` when conversation is not owned by `userId`; new IDOR regression test inserts other-user conv then asserts `null`; 22/22 chat tests pass                                                                                                                   |
| H12 | CookSessionCapture auto-analyze path: `handleAnalyzePhoto(initialPhotoUri)` runs `ensureSession() → setSessionId(sid) → addPhoto.mutateAsync(photoUri)` in same microtask. `setSessionId` doesn't re-render before `mutateAsync` resolves, so the stale closure captures `sessionId = null` and throws "No active session" | data-integrity, code-quality | Camera                 | `client/screens/CookSessionCaptureScreen.tsx:86-114`, `client/hooks/useCookSession.ts:42-87` | verified | Refactored `useAddCookPhoto` to take `sessionId` as a mutation variable (not hook arg); `CookSessionCaptureScreen.handleAnalyzePhoto` threads the freshly-returned `sid` through `mutateAsync({ sessionId: sid, photoUri })`; type check passes; 9 related test files pass (315 tests)                                                                                  |

### Medium

| ID  | Finding                                                                                                                                                                                                                                           | Domain                       | Agent                  | File(s)                                                                                                                                     | Status   | Verification       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------ |
| M1  | URL-imported recipe `imageUrl` not protocol-restricted — `javascript:`/`data:` accepted into `mealPlanRecipes.imageUrl`. Low blast now (RN Image ignores unknown schemes); Medium when web frontend lands                                         | security                     | Security               | `server/services/recipe-import.ts:28,507-509`                                                                                               | deferred | see Deferred Items |
| M2  | Coach `search_recipes` tool calls `searchCatalogRecipes(...)` without the user's `intolerances` parameter. Coach Pro can suggest peanut-containing recipes to a peanut-allergic user                                                              | security, data-integrity     | Security, AI/LLM       | `server/services/coach-tools.ts:334-343`                                                                                                    | deferred | see Deferred Items |
| M3  | `POST /api/coach/warm-up` doesn't verify `conversation.type === "coach"` — warming a `recipe`/`remix` conversation wastes a warm-up slot                                                                                                          | security, code-quality       | Security               | `server/routes/coach-context.ts:89-159`                                                                                                     | deferred | see Deferred Items |
| M4  | `createNotebookEntries` warn-log includes raw `userId` verbatim — every dedupeKey-missing write leaks per-user activity to log aggregator                                                                                                         | security                     | Security               | `server/storage/coach-notebook.ts:61-71`                                                                                                    | deferred | see Deferred Items |
| M5  | `evals/results/*.json` persists raw coach responses + user messages with no secrets-scanner gate (pre-commit scans datasets only)                                                                                                                 | security                     | Security               | `evals/runner.ts:586-594`                                                                                                                   | deferred | see Deferred Items |
| M6  | Cached coach responses replayed without re-running `containsDangerousDietaryAdvice` — pattern tightening doesn't invalidate cache for ≤7d                                                                                                         | security                     | AI/LLM                 | `server/services/coach-pro-chat.ts:270-285`                                                                                                 | deferred | see Deferred Items |
| M7  | Judge `judgeResponseSchema` accepts partial `scores` array — missing dimensions silently drop, biasing aggregates                                                                                                                                 | data-integrity               | AI/LLM                 | `evals/judge.ts:13-22`, `evals/runner.ts:222-231`                                                                                           | deferred | see Deferred Items |
| M8  | Client-disconnect sets `aborted=true` but doesn't abort OpenAI stream — tokens continue billing after user cancel                                                                                                                                 | performance, data-integrity  | AI/LLM                 | `server/services/nutrition-coach.ts:166-178,252-264`, `server/routes/chat.ts:296-300`                                                       | deferred | see Deferred Items |
| M9  | `shouldUpdateStrategy(0)` returns false — self-locking gate: new users never get `coaching_strategy` extracted                                                                                                                                    | data-integrity, code-quality | AI/LLM                 | `server/services/notebook-extraction.ts:103-105`                                                                                            | deferred | see Deferred Items |
| M10 | Eval judge prompt interpolates `dietaryProfile.allergies/dislikes` into `<user_context>` without sanitization (production coach-side sanitizes; parity gap)                                                                                       | security                     | AI/LLM                 | `evals/judge.ts:102-167`                                                                                                                    | deferred | see Deferred Items |
| M11 | Coach tool args parsed with `JSON.parse` then passed as `Record<string, unknown>` to `executeToolCall` with no Zod validation; `log_food_item` handler reads `mealType`/`description` not declared in tool schema (phantom params)                | data-integrity, code-quality | AI/LLM                 | `server/services/coach-tools.ts:317-465`, `server/services/nutrition-coach.ts:359`                                                          | deferred | see Deferred Items |
| M12 | Notebook injected into system prompt with `<notebook_entry>` tags but no "UNTRUSTED DATA — not instructions" directive (judge prompt has this). Stored-prompt-injection vector via adversarial notebook seeding                                   | security                     | AI/LLM                 | `server/services/coach-pro-chat.ts:213-233`, `server/services/nutrition-coach.ts:138-144`                                                   | deferred | see Deferred Items |
| M13 | `saveRecipeFromChat` casts safeParse result back to `Record<string, unknown>`, reads `rawMetadata.savedRecipeId`, `Number()`-coerces → NaN/0; no `authorId`/`isPublic` filter on lookup (latent IDOR)                                             | data-integrity, security     | Database               | `server/storage/chat.ts:382-392`                                                                                                            | deferred | see Deferred Items |
| M14 | `getChatConversations` orders by `updatedAt DESC` + filters by `userId` with no composite index — index scan + in-memory sort on every list fetch                                                                                                 | performance                  | Database               | `server/storage/chat.ts:33-48`                                                                                                              | deferred | see Deferred Items |
| M15 | `createChatMessageWithLimitCheck` runs two independent count queries serially inside advisory-lock tx — `Promise.all` halves lock-hold time                                                                                                       | performance                  | Database               | `server/storage/chat.ts:198-213,215-232,246-261,263-280`                                                                                    | deferred | see Deferred Items |
| M16 | `coach_notebook.dedupeKey` unique index is nullable + not partitioned by `status` — NULL-distinct means `onConflictDoNothing` doesn't dedup NULL-keyed rows; archived identical entries re-blocked after 30d                                      | data-integrity               | Database               | `shared/schema.ts:1439,1461-1463`, `server/storage/coach-notebook.ts:43-82`                                                                 | deferred | see Deferred Items |
| M17 | `deleteUser` purges community recipes in tx but doesn't call `removeFromIndex` → deleted recipes keep surfacing in search until restart                                                                                                           | data-integrity               | Database               | `server/storage/users.ts:176-206`                                                                                                           | deferred | see Deferred Items |
| M18 | `SessionStore.createWithKey` calls `clearSession(key)` before the cap check → if cap rejects, the previous warm-up is already wiped                                                                                                               | data-integrity               | Database               | `server/storage/sessions.ts:148-182`                                                                                                        | deferred | see Deferred Items |
| M19 | `getCoachCachedResponse` does `db.select()` instead of `.select({ id, response })` — ships full `question` TEXT + timestamps on every cache hit                                                                                                   | performance                  | Database               | `server/storage/chat.ts:447-470`                                                                                                            | deferred | see Deferred Items |
| M20 | `getAllRecipeIngredients` does full `.select()` — only `name` needed by search index; ships `quantity`/`unit`/`category`/`displayOrder` on boot                                                                                                   | performance                  | Database               | `server/storage/meal-plans.ts:333-351`                                                                                                      | deferred | see Deferred Items |
| M21 | Seed script inserts directly via `db.insert(communityRecipes)` instead of `storage.createCommunityRecipe` — skips `addToIndex` call; new seeds invisible until restart                                                                            | data-integrity               | Database               | `server/scripts/seed-recipes.ts:350-369`                                                                                                    | deferred | see Deferred Items |
| M22 | `maxPrepTime` query param filters `totalTimeMinutes` (prep+cook) — a crockpot recipe with 10 min prep + 6h cook is excluded from `maxPrepTime=15`                                                                                                 | data-integrity               | Nutrition              | `server/services/recipe-search.ts:251-256`, `server/routes/recipe-search.ts:24`                                                             | deferred | see Deferred Items |
| M23 | URL-imported nutrition `parseNutritionValue(/([\d.]+)/)` strips units — `"1046 kJ"` silently stored as 1046 kcal (4.18× real calories)                                                                                                            | data-integrity               | Nutrition              | `server/services/recipe-import.ts:138-142,528-531`                                                                                          | deferred | see Deferred Items |
| M24 | URL imports often have no nutrition in schema.org; `parseNutritionValue` returns null → recipes silently excluded from macro-filtered searches with no user-facing explanation                                                                    | data-integrity, code-quality | Nutrition              | `server/services/recipe-search.ts:258-271`, `server/routes/recipe-import.ts:208-211`                                                        | deferred | see Deferred Items |
| M25 | `weightLogs` has no unit column; `weight-trend` math `parseFloat(weight)` assumes consistent kg/lb. iOS HealthKit sync (kg) + manual UI (lb) produces nonsense projections                                                                        | data-integrity               | Nutrition              | `shared/schema.ts:792-812`, `server/services/weight-trend.ts:35-126`                                                                        | deferred | see Deferred Items |
| M26 | Nine catch blocks in `recipes.ts`, `recipe-catalog.ts`, `recipe-search.ts` use legacy `logger.error + sendError` instead of `handleRouteError` (split refactor missed them)                                                                       | architecture, code-quality   | Code-Reviewer          | `server/routes/recipes.ts:62,101,134,312,333,372,408`, `recipe-catalog.ts:97,138`, `recipe-search.ts:65,114`                                | deferred | see Deferred Items |
| M27 | `recipe-import.ts` passes `result.error` (a free-text string like `"FETCH_FAILED"`) as the `ErrorCode` arg to `sendError` — not in the `ErrorCode` enum; clients can't reliably switch on codes                                                   | code-quality                 | Code-Reviewer          | `server/routes/recipe-import.ts:71,146`                                                                                                     | deferred | see Deferred Items |
| M28 | Six new/split route files declare Zod schemas inline (`catalogSearchSchema`, `importUrlSchema`, `recipeGenerationSchema`, `searchQuerySchema`, `browseQuerySchema`, `generatePromptSchema`) — none in `shared/schemas/recipe.ts` for client reuse | architecture, typescript     | Code-Reviewer          | `server/routes/recipe-catalog.ts:54-62`, `recipe-import.ts:24-36`, `recipes.ts:33-43`, `recipe-search.ts:13-41`, `recipe-generate.ts:15-17` | deferred | see Deferred Items |
| M29 | `POST /api/meal-plan/recipes/parse-url` (preview endpoint, 63 LOC) has zero route tests — new endpoint, complex URL/SSRF/error-mapping logic                                                                                                      | code-quality                 | Testing, Code-Reviewer | `server/routes/recipe-import.ts:40-103`, `server/routes/__tests__/recipes.test.ts`                                                          | deferred | see Deferred Items |
| M30 | `PreviewStep.EditButton` (6 uses) has `fontSize: 13` + `hitSlop={8}` → effective ~29-31px. WCAG 2.5.5 requires 44×44. Sibling `InstructionsStep` reorder buttons were fixed to 44×44 in prior M19 — this drifted                                  | code-quality                 | RN UI/UX               | `client/components/recipe-wizard/PreviewStep.tsx:36-47,468-471`                                                                             | deferred | see Deferred Items |
| M31 | `TimeServingsStep.stepperButton` is `width: 36, height: 36` (visible); iOS HIG + WCAG 2.5.8 recommend 44×44 visible. Inconsistent with M19 fix                                                                                                    | code-quality                 | RN UI/UX               | `client/components/recipe-wizard/TimeServingsStep.tsx:98-108,117-130,227-234`                                                               | deferred | see Deferred Items |
| M32 | Servings stepper has no `accessibilityValue` / `accessibilityRole="adjustable"` — VoiceOver users pressing +/− get no value announcement                                                                                                          | code-quality                 | RN UI/UX               | `client/components/recipe-wizard/TimeServingsStep.tsx:94-131`                                                                               | deferred | see Deferred Items |
| M33 | ScanScreen `useCamera.onBarcodeScanned` callback calls `haptics.notification(Success)` explicitly AND `triggerScanFlash()` which internally fires `Haptics.notificationAsync(Success)` — double haptic per scan                                   | code-quality                 | Camera                 | `client/screens/ScanScreen.tsx:140-146`, `client/hooks/useSuccessAnimation.ts:37-49`                                                        | deferred | see Deferred Items |
| M34 | `coachProInternals.lastArchivedAt` Map never cleared in `beforeEach` of `handleCoachChat` test block — all tests use `userId: "user-42"`, later tests silently skip archive branch; latent ordering dep                                           | code-quality                 | Testing                | `server/services/__tests__/coach-pro-chat.test.ts:207-717`                                                                                  | deferred | see Deferred Items |
| M35 | `asMockReturn<T>(value: Partial<T>): T { return value as T }` — plain cast defeats factory pattern; used ~10×                                                                                                                                     | code-quality                 | Testing                | `server/services/__tests__/coach-pro-chat.test.ts:174-176`                                                                                  | deferred | see Deferred Items |
| M36 | `coach-context.test.ts` inlines full `User`/`UserProfile`/`CoachNotebookEntry`/`ChatConversation` objects 3× instead of using `createMockUser`, `createMockUserProfile` factories                                                                 | code-quality                 | Testing                | `server/routes/__tests__/coach-context.test.ts:66-142,206-251,289-307`                                                                      | deferred | see Deferred Items |
| M37 | `weight-trend.test.ts` uses `makeWeightEntry(weight, daysAgo)` with `new Date()` at call time — tests on DST boundaries or ~midnight UTC can bucket entries wrong                                                                                 | code-quality                 | Testing                | `server/services/__tests__/weight-trend.test.ts:4-12,52-90,100-119`                                                                         | deferred | see Deferred Items |
| M38 | `chat.test.ts:166-178` uses `setTimeout(r, 10)` to force a timestamp gap before asserting `>= before.getTime()` — tautological assertion, real-time sleep in test                                                                                 | code-quality                 | Testing                | `server/storage/__tests__/chat.test.ts:166-178`                                                                                             | deferred | see Deferred Items |
| M39 | `backfillMealTypes`, `server/lib/meal-type-inference.ts`, and `server/routes/_recipe-helpers.ts` (`stripAuthorId`/`stripAuthorIdOne`) have no direct tests — only covered indirectly via re-exports                                               | code-quality                 | Testing                | `server/services/meal-type-inference.ts:14-27`, `server/lib/meal-type-inference.ts`, `server/routes/_recipe-helpers.ts:7-19`                | deferred | see Deferred Items |
| M40 | `server/services/coach-pro-chat.ts:205-208` plumbs `profile.allergies`/`foodDislikes` into `CoachContext` without `sanitizeContextField` — same file sanitizes notebook entries at line 219 (inconsistent)                                        | security                     | Architecture           | `server/services/coach-pro-chat.ts:205-208`                                                                                                 | deferred | see Deferred Items |

### Low

| ID  | Finding                                                                                                                                                   | Status   | Notes                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------- |
| L1  | `getCoachCachedResponse` hit-count `.catch(() => {})` swallows DB errors indefinitely                                                                     | deferred | see Deferred Items                     |
| L2  | `buildJudgePrompt` doesn't escape `</coach_response>` literals in response body before interpolation                                                      | deferred | see Deferred Items                     |
| L3  | `calculateWeeklyRate` silently returns `NaN` on unparseable weight strings (coach context interpolates `"NaNkg/week"`)                                    | deferred | see Deferred Items                     |
| L4  | `parseBlocksFromContent` regex `/```coach_blocks\n([\s\S]*?)```/` — theoretical ReDoS on unterminated fence; bounded by 1500-token max                    | deferred | see Deferred Items                     |
| L5  | Spoonacular `findNutrient` doesn't validate `.unit` field — vulnerable if API contract shifts (kcal→kJ)                                                   | deferred | see Deferred Items                     |
| L6  | Seed recipes have no nutrition data (no column in `community_recipes`) — ships 25 recipes with zero macros for every demo user                            | deferred | see Deferred Items (compound with H10) |
| L7  | Meal-type keyword heuristic: `salad`/`soup` only in lunch (dinner salads/stews common); `muffin` breakfast+snack; no "breakfast burrito"                  | deferred | see Deferred Items                     |
| L8  | `source=spoonacular` accepted by search schema but MiniSearch index doesn't include it → silent `{results: [], total: 0}`                                 | deferred | see Deferred Items                     |
| L9  | `parseNutritionValue` regex accepts `"-10"` and returns `10` (stripped negative)                                                                          | deferred | see Deferred Items                     |
| L10 | `inferMealTypes` returns all four meal types on no-keyword-match — defeats the GIN index for "unclassified" recipes                                       | deferred | see Deferred Items                     |
| L11 | `StandaloneSkeletonBox` fallback (outside SkeletonProvider) still creates `useAnimatedStyle` under reducedMotion (held constant)                          | deferred | see Deferred Items                     |
| L12 | `InstructionsStep.handleMove` fires haptic but no `AccessibilityInfo.announceForAccessibility` (sibling `handleRemove` does)                              | deferred | see Deferred Items                     |
| L13 | `WizardShell.editFromPreview` jumps steps without SR announcement ("Step N of 7")                                                                         | deferred | see Deferred Items                     |
| L14 | Wizard slide animation direction is "forward" when returning to Preview from edit-step (semantically feels backward)                                      | deferred | see Deferred Items                     |
| L15 | `IngredientsStep.inputRefs` Map is wired but never read — dead code or unimplemented auto-focus                                                           | deferred | see Deferred Items                     |
| L16 | `SearchFilterSheet` sliders fire only `onSlidingComplete` — no live SR feedback during drag                                                               | deferred | see Deferred Items                     |
| L17 | `useCamera.scannedBarcodesRef` Map grows unbounded in batch mode (bounded in practice by BatchScanScreen's MAX_ITEMS=50)                                  | deferred | see Deferred Items                     |
| L18 | `BatchScanScreen` doesn't call `resetScanning()` alongside `startSession()` — stale map timestamps could suppress re-scans on fast refresh                | deferred | see Deferred Items                     |
| L19 | `CookSessionCaptureScreen` sets `isActive={false}` during analyzing — preview freezes behind spinner overlay                                              | deferred | see Deferred Items                     |
| L20 | `useScanClassification.isClassifyingRef` not reset on blur — in-flight upload can navigate user from a different screen                                   | deferred | see Deferred Items                     |
| L21 | `ScanScreen.onBarcodeScanned` callback not memoized — re-registers native scanner every render                                                            | deferred | see Deferred Items                     |
| L22 | `concurrent initSearchIndex` race test actually calls `resetSearchIndex` in both `beforeEach` AND test body — not genuinely racy                          | deferred | see Deferred Items                     |
| L23 | `recipes.test.ts` mocks `CatalogQuotaError` as a fresh class — `instanceof` checks at route boundary would fail                                           | deferred | see Deferred Items                     |
| L24 | `coach-pro-chat.test.ts` userId-cache-regression test reuses `vi.clearAllMocks()` mid-test + `content:"Hello"` — weaker than direct pure test             | deferred | see Deferred Items                     |
| L25 | `community.test.ts` / `meal-plans.test.ts` use `featured.some(r => r.title === X)` patterns — can't detect extra unexpected recipes                       | deferred | see Deferred Items                     |
| L26 | `check-eval-dataset-secrets.test.ts` `mkdtempSync` temp dirs never cleaned — `/tmp/eval-secrets-check-*` accumulates on CI runners                        | deferred | see Deferred Items                     |
| L27 | `inferMealTypes`: `"wrap"`/`"bowl"` appear in both `MEAL_TYPE_KEYWORDS.lunch` and `MULTI_TYPE_OVERRIDES` — dedup'd by Set, but dead entries               | deferred | see Deferred Items                     |
| L28 | `getRecipesWithEmptyMealTypes`/`getCommunityRecipesWithEmptyMealTypes` don't check `discardedAt` — will silently re-tag soft-deleted rows if column added | deferred | see Deferred Items                     |
| L29 | `getPremiumFeatures` called on every `generation-status` poll — subscription tier could be cached ~1min                                                   | deferred | see Deferred Items                     |
| L30 | `recipe-generate.ts:27` comment claims "same contract as POST /api/recipes/generate" — but doesn't log generation (see H1). Comment inaccurate            | deferred | see Deferred Items                     |
| L31 | Advisory lock `pg_advisory_xact_lock(hashtext(userId))` uses 32-bit int; collisions at ~65k users (>50% birthday). Noise today                            | deferred | see Deferred Items                     |

## Deferred Items

| Findings                                                           | Todo                                                  | Rationale                                                                                                                                                         |
| ------------------------------------------------------------------ | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1, M2, M3, M4, M5, M7, M10, M12, L1, L2, L4                       | `todos/security-followups-2026-04-18.md`              | 11-item security hardening: URL protocol, allergen tool plumbing, warm-up type check, log PII, judge sanitize/escape/fail-closed, notebook directive, ReDoS guard |
| M6, M8, M9, M11, M13, M14, M15, M16, M17, M40, L19, L20            | `todos/coach-followups-2026-04-18.md`                 | 12-item coach/chat hardening: cached-resp safety re-scan, stream abort, strategy gate, tool Zod, IDOR, composite index, partial index                             |
| H10-followup, L6, M22, M23, M24, M25, L3, L5, L7, L8, L9, L10, L27 | `todos/nutrition-accuracy-2026-04-18.md`              | Nutrition accuracy: community nutrition schema + backfill, weightLogs unit, filter semantics, kJ parsing, meal-type edge cases                                    |
| M26, M27, M28, M29, L29, L30                                       | `todos/recipe-routes-consistency-2026-04-18.md`       | Route-split drift: handleRouteError migration, ErrorCode enum, shared Zod schemas, parse-url tests                                                                |
| M30, M31, M32, L11, L12, L13, L14, L15, L16                        | `todos/wizard-a11y-v3-2026-04-18.md`                  | Wizard v3 polish: PreviewStep tap targets, stepper size + value, SR announcements, animation direction                                                            |
| M34, M35, M36, M37, M38, M39, L22, L23, L24, L25, L26              | `todos/testing-hygiene-2026-04-18.md`                 | Test hygiene: global-state reset, factory adoption, fake timers, coverage, mock identity                                                                          |
| M33, L17, L18, L21                                                 | `todos/camera-scan-polish-2026-04-18.md`              | Camera polish: double haptic, unbounded ref, resetScanning, callback memo                                                                                         |
| M18, M19, M20, M21, L28, L31                                       | `todos/db-perf-followups-2026-04-18.md`               | DB hygiene: session-store cap ordering, column projection, seed index routing, advisory-lock key size                                                             |
| H9-partial                                                         | `todos/architecture-warm-up-relocation-2026-04-18.md` | Move `warmUpStore` instance from `services/` to `storage/sessions.ts` — deferred because it's broader refactor than the `_internals` fix                          |

_Note: `todos/` is gitignored; todo files live locally as durable work tickets. Finding IDs cross-reference the manifest above._

## Summary

| Severity  | Found | Verified | Deferred | False-positive | Open  |
| --------- | ----- | -------- | -------- | -------------- | ----- |
| Critical  | 0     | 0        | 0        | 0              | 0     |
| High      | 12    | 12       | 0        | 0              | 0     |
| Medium    | 40    | 0        | 40       | 0              | 0     |
| Low       | 31    | 0        | 31       | 0              | 0     |
| **Total** | 83    | 12       | 71       | 0              | **0** |

_All findings are now verified, deferred (with linked todo), or false-positive. Zero open findings at close — the audit is formally complete per the skill's Phase 5 closure rule._

## Fix Commits

| Commit | Description |
| ------ | ----------- |
| —      | —           |
