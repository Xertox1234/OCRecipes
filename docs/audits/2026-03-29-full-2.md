# Audit: Full Audit (Round 2)

> **Date:** 2026-03-29
> **Trigger:** Periodic full codebase audit (all 5 domains reporting)
> **Domains:** security, performance, data-integrity, architecture, code-quality
> **Baseline:** 3169 tests passing (223 files) | 1 type error | 0 lint errors, 4 warnings

## Findings

### Critical

| ID  | Finding              | File(s) | Status | Verification |
| --- | -------------------- | ------- | ------ | ------------ |
| —   | No critical findings | —       | —      | —            |

### High

| ID  | Finding                                                                                                                                              | File(s)                                              | Status   | Verification                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | AI prompt injection: `suggestions.ts` interpolates unsanitized `dietType`, `cookingSkillLevel`, `cookingTimeAvailable`, allergy names into AI prompt | `server/routes/suggestions.ts:86-104`                | verified | grep confirms 11 sanitizeUserInput calls (both suggestion + instruction paths); 17/17 tests pass                                         |
| H2  | AI prompt injection: `buildDietaryContext()` in recipe-generation interpolates unsanitized profile fields + allergy names                            | `server/services/recipe-generation.ts:77-110`        | verified | grep confirms 7 sanitizeUserInput calls (allergies, dietType, cookingSkill, cookingTime, additionalPrefs, productName); 20/20 tests pass |
| H3  | AI prompt injection: `buildDietaryProfileSummary()` interpolates unsanitized `dietType`, allergies, `foodDislikes`, `primaryGoal`                    | `server/services/ingredient-substitution.ts:209-230` | verified | grep confirms 6 sanitizeUserInput calls (per-field + whole-string defense-in-depth); 31/31 tests pass                                    |

### Medium

| ID  | Finding                                                                                                                                                       | File(s)                                                                                    | Status   | Verification                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------- |
| M1  | Unsanitized user input in DALL-E image generation prompt (`productName` raw at line 206, only sanitized for text gen at line 128)                             | `server/services/recipe-generation.ts:201-206,246`                                         | verified | grep confirms sanitizeUserInput on both recipeTitle and productName in DALL-E prompt; 20/20 tests pass               |
| M2  | ~14 authenticated routes missing rate limiting (recipes: 7 endpoints, meal-plan: 4, menu: 2, receipt: 2)                                                      | `server/routes/recipes.ts`, `meal-plan.ts`, `menu.ts`, `receipt.ts`                        | verified | crudRateLimit added to 15 endpoints across 4 files; 137/137 tests pass                                               |
| M3  | Fasting start TOCTOU: no unique partial index on `fastingLogs(userId) WHERE ended_at IS NULL` — concurrent requests can create duplicate active fasts         | `server/routes/fasting.ts:92-110`, `shared/schema.ts:888-909`                              | verified | uniqueIndex `fasting_logs_one_active_idx` added + 23505 catch returns 409; 17/17 tests pass                          |
| M4  | `suggestionCache` allows duplicate rows for same `(scannedItemId, userId, profileHash)` — no unique constraint, plain INSERT                                  | `server/storage/cache.ts:43-61`, `shared/schema.ts:242-267`                                | verified | uniqueIndex `suggestion_cache_item_user_profile_idx` + onConflictDoUpdate in createSuggestionCache; 17/17 tests pass |
| M5  | `getMealPlanItems` batch-fetches recipes then scanned items sequentially — two independent queries not parallelized                                           | `server/storage/meal-plans.ts:332-350`                                                     | verified | Promise.all wraps both batch fetches; 53/53 meal-plan tests pass                                                     |
| M6  | Ad-hoc error codes outside `ErrorCode` constant: 7 unique string literals in 5 route files (`DAILY_LIMIT_REACHED`, `NOT_IN_DATABASE`, `ITEM_NOT_FOUND`, etc.) | `server/routes/nutrition.ts`, `cooking.ts`, `recipes.ts`, `meal-suggestions.ts`, `chat.ts` | verified | 3 codes added to ErrorCode, 4 replaced with semantic equivalents; 167/167 tests pass                                 |
| M7  | ChatListScreen: `FadeInDown.delay(index * 50)` with no cap (50 convos = 2.5s delay for last item)                                                             | `client/screens/ChatListScreen.tsx:127`                                                    | verified | MAX_ANIMATED_INDEX = 10 cap added (matching HistoryScreen pattern); lint passes                                      |

### Low

| ID  | Finding                                                                                                              | File(s)                                                                                                                              | Status   | Verification                                                                                                           |
| --- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------- |
| L1  | `lookupBarcode` god function (~310 lines, 5 distinct steps) in a 1214-line file                                      | `server/services/nutrition-lookup.ts:810-1117`                                                                                       | deferred | Complex refactor with regression risk — needs dedicated PR                                                             |
| L2  | ZodError catch boilerplate repeated 25 times across 12 route files — should be a shared helper                       | Multiple route files                                                                                                                 | verified | `handleRouteError` helper added to `_helpers.ts`; 26 instances replaced across 13 files; 3169/3169 tests pass          |
| L3  | Services bypass storage facade: `nutrition-lookup.ts` and `meal-type-inference.ts` import `db` directly              | `server/services/nutrition-lookup.ts:3`, `meal-type-inference.ts:1`                                                                  | deferred | Architectural refactor — needs careful coordination with storage module decomposition (L4)                             |
| L4  | `meal-plans.ts` storage module oversized (979 lines, 7 domain concerns) — threshold is ~500                          | `server/storage/meal-plans.ts`                                                                                                       | deferred | Structural refactor into 3 modules — needs dedicated PR to avoid merge conflicts                                       |
| L5  | `goals/calculate` route makes 3 sequential DB calls (updateUser, getUserProfile, updateProfile/createProfile)        | `server/routes/goals.ts:61-86`                                                                                                       | verified | Promise.all with upsertProfileWithOnboarding reduces 3 calls to 2 parallel; 11/11 tests pass                           |
| L6  | Grocery IDOR checks fetch entire list with items just for ownership verification                                     | `server/routes/grocery.ts:280-283,363-367,419-424`                                                                                   | deferred | Needs new lightweight `getGroceryList` storage function — not mechanical                                               |
| L7  | `Record<string, any>` — sole `any` in production server code (Open Food Facts response)                              | `server/services/nutrition-lookup.ts:816`                                                                                            | deferred | Defining OFF interface requires mapping ~12 deeply nested fields — couples with L1 god function refactor               |
| L8  | Raw `Number()` instead of `parseQueryInt` helper in featured recipes endpoint                                        | `server/routes/recipes.ts:112-113`                                                                                                   | verified | Replaced with parseQueryInt; 52/52 tests pass                                                                          |
| L9  | Admin routes use string literals instead of `ErrorCode` import (14 `sendError` calls)                                | `server/routes/admin-api-keys.ts`                                                                                                    | verified | All 14 sendError calls now use ErrorCode constant; 10/10 tests pass                                                    |
| L10 | 6 FlatList screens missing `FLATLIST_DEFAULTS` spread (`removeClippedSubviews`, `maxToRenderPerBatch`, `windowSize`) | `ChatListScreen.tsx`, `CookbookDetailScreen.tsx`, `CookbookListScreen.tsx`, `GroceryListsScreen.tsx`, `SubstitutionResultScreen.tsx` | verified | FLATLIST_DEFAULTS spread added to 5 FlatList screens (GroceryListScreen uses SectionList); lint passes                 |
| L11 | Numeric string coercion Zod schema pattern repeated 15 times — should be extracted to reusable schema                | `server/routes/nutrition.ts` (7x), `meal-plan.ts` (6x), `pantry.ts`, `grocery.ts`                                                    | verified | `numericStringField` + `nullableNumericStringField` helpers in \_helpers.ts; 15 instances replaced; 143/143 tests pass |
| L12 | User-to-JSON serialization duplicated 4 times in auth route handlers                                                 | `server/routes/auth.ts:83-93,148-156,211-219,253-261`                                                                                | verified | `serializeUser()` helper extracts shared mapping; 74/74 tests pass                                                     |

## Deferred Items

| ID  | Todo                    | Rationale                                                                                                               |
| --- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| L1  | Deferred (tracked here) | `lookupBarcode` god function refactor (~310 lines) — complex structural change with regression risk, needs dedicated PR |
| L3  | Deferred (tracked here) | Services bypass storage facade — architectural refactor needs coordination with L4 storage module decomposition         |
| L4  | Deferred (tracked here) | `meal-plans.ts` 979 lines → split into 3 modules — needs dedicated PR to avoid merge conflicts                          |
| L6  | Deferred (tracked here) | Grocery IDOR overfetch — needs new lightweight `getGroceryList` storage function                                        |
| L7  | Deferred (tracked here) | `Record<string, any>` in nutrition-lookup — couples with L1 god function refactor                                       |

## Summary

| Severity  | Found  | Verified | Deferred | False-positive | Open  |
| --------- | ------ | -------- | -------- | -------------- | ----- |
| Critical  | 0      | 0        | 0        | 0              | 0     |
| High      | 3      | 3        | 0        | 0              | 0     |
| Medium    | 7      | 7        | 0        | 0              | 0     |
| Low       | 12     | 7        | 5        | 0              | 0     |
| **Total** | **22** | **17**   | **5**    | **0**          | **0** |

## Fix Commits

| Commit | Description |
| ------ | ----------- |

## Codification (Phase 7)

### Patterns Extracted

| Finding | Pattern | Added To |
| ------- | ------- | -------- |

### Learnings Extracted

| Finding | Learning Title | Category |
| ------- | -------------- | -------- |

### Code Reviewer Updates

| Finding | New Check Added |
| ------- | --------------- |

## Post-Audit Notes

- First audit where all 5 domains reported successfully (architecture and code-quality hit rate limits in both previous audits)
- Thematic patterns: (1) AI prompt sanitization still incomplete on profile fields (H1-H3, M1), (2) rate limiting gaps on read-heavy endpoints (M2), (3) TOCTOU race conditions on lower-traffic features (M3, M4), (4) sequential DB queries easily parallelizable (M5), (5) code quality debt in shared patterns (M6, L2, L11, L12)
