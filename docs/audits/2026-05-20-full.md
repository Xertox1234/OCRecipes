# Audit: Full Codebase Audit (changes since 2026-05-16)

> **Date:** 2026-05-20
> **Trigger:** Periodic full audit. Scoped to source changed since the 2026-05-16 full audit — ~228 files / +5969/-1960 across `client/`, `server/`, `shared/`. Major areas: verification streak premium unlocks (#224), meal-plan nutrition-source XOR DB constraint (#227), subscription tier cache + premium-expiry gating (#226), `shared/constants/allergens.ts` + `shared/lib/units.ts` (new), recipe-search filter additions, pantry-meal-plan service, coach safety-refusal refinements, type-aware ESLint burndown (#228–231), pgTable array-form migration in `shared/schema.ts` (#229), LSP-utilization tooling/docs (#233).
> **Domains:** security, performance, data-integrity, architecture, code-quality, camera, accessibility
> **Baseline:** CI green at `6b833ad1` (HEAD `d7cdebd9`/`f81f6333` on top are docs/todos-only) — tests passing, 0 type errors, 0 lint errors. Baseline taken from the CI gate rather than a local `test:run`/`check:types`/`lint` run, per CLAUDE.md's rule against self-verifying at session start. CI run: `26202756966` (success, 2026-05-21).
> **Worktree:** `.worktrees/audit-2026-05-20` (from HEAD `d7cdebd9`); `.env` + `node_modules` linked from main checkout.

## Dedup base

Findings deduped against the three most recent audits and the todos resolved in the audit window:

- `docs/audits/2026-05-16-full.md` (periodic full)
- `docs/audits/2026-05-16-unfinished-features.md` (custom scope; 4 items deferred to todos — several since resolved)
- `docs/audits/2026-05-18-kimi.md` (kimi pipeline)

Already-resolved (archived) todos in this window — agents instructed NOT to re-flag:
verification-streak-premium-unlocks, meal-plan-source-xor-db-constraint, tier-cache-premium-expiry-gap, subscription-status-parallelize-streak-query, verification-presubmit-ismatch-race, verification-reformulation-lock-race-test, coach-safety-refusal-\*, recipe-search-hooks-zod-response-validation, async-lint burndowns.

## Findings

Each finding has a lifecycle: `open` → `fixing` → `verified` or `deferred` or `false-positive`.

**Status key:** `open` · `fixing` · `verified` · `deferred` (link todo) · `false-positive`

**Research key** (Phase 2.5): `confirmed` · `better-fix` · `contradicted ⚠` · `—`

### Critical

_None._

### High

_None._

### Medium

| ID  | Finding                                                                                                                                                                                                                                                                                                                                                   | Domain         | Agent                    | File(s)                                                                           | Research  | Status   | Verification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------ | --------------------------------------------------------------------------------- | --------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | Allergen detection false-positives on plant-based substitutes — single-word keywords `flour`/`milk`/`cream`/`butter` match "almond flour", "oat milk", "coconut cream", "peanut butter" → tag substitutes with the allergen they replace. Poisons denormalized `allergens` cache; degrades `safeForMe` filter for the dietary-restricted users it serves. | data-integrity | database-specialist      | `shared/constants/allergens.ts:108-220` (matcher `ingredientContainsKeyword:642`) | —         | verified | 51/51 allergens tests pass incl. must-still-flag negatives (whole milk, wheat flour, buttermilk, ice cream, white/bread/all-purpose flour) + substitute cases (almond milk→tree_nut not dairy; soy milk→soy; peanut butter→peanut). Strictly-plant modifier set excludes goat/sheep/buffalo/camel (no under-flag). kimi WARNING (multi-token "soy-based milk" still flags — safe over-flag direction) → Deferred Items. **Cache staleness:** existing `mealPlanRecipes`/`communityRecipes.allergens` rows are stale until `backfill-recipe-allergens.ts` is re-run (dev/test only; no prod data — see [[L4]]). |
| M2  | Non-null assertion `analysisResult.sessionId!` on a value typed `string \| null`; if server returns null it ships `null` into the follow-up/confirm request instead of failing closed.                                                                                                                                                                    | code-quality   | quality-specialist       | `client/hooks/usePhotoAnalysis.ts:251,297` (type `client/lib/photo-upload.ts:44`) | confirmed | verified | Both `sessionId!` removed; follow-up skips on null session, log fails closed with user-visible error; `sessionId` captured in const post-guard (narrowing-safe past `setIsConfirming`). Type-clean (only pre-existing `deleteAsync` deprecation remains). No hook test file. kimi-review: no findings                                                                                                                                                                                                                                                                                                          |
| M3  | Weight + goal `TextInput`s have `weightError`/`goalError` + `InlineError` but no `aria-invalid`; screen readers can't detect invalid state. Sibling `WeightLogDrawer.tsx:354` does it correctly.                                                                                                                                                          | accessibility  | accessibility-specialist | `client/screens/WeightTrackingScreen.tsx:160,333`                                 | confirmed | verified | `aria-invalid={weightError!=null}` / `{goalError!=null}` on both inputs (matches `WeightLogDrawer` pattern). 0 IDE diagnostics; kimi-review no CRITICAL. VoiceOver UI check not runnable in this env                                                                                                                                                                                                                                                                                                                                                                                                           |
| M4  | RecipeChatScreen has zero `announceForAccessibility` — stream-completion and recipe save success/error are silent (haptics only). ChatScreen announces the same transitions (2 calls).                                                                                                                                                                    | accessibility  | accessibility-specialist | `client/screens/RecipeChatScreen.tsx:244-261,277-292`                             | confirmed | verified | `AccessibilityInfo` imported; "Recipe response received" inside the `wasStreamingRef && !isStreaming` guard (no mount announce, no liveRegion pairing); save success/error announced in `handleSaveRecipe`. 0 diagnostics. kimi-review: no findings                                                                                                                                                                                                                                                                                                                                                            |
| M5  | RecipeBrowserScreen's three new results-swap branches (quota-exceeded, online-error, blank-online) replace the list with no `announceForAccessibility` — VoiceOver/TalkBack user gets silence on search failure.                                                                                                                                          | accessibility  | accessibility-specialist | `client/screens/meal-plan/RecipeBrowserScreen.tsx:790-820`                        | confirmed | verified | Effect announces quota/online-error transitions; tracks announced error _kind_ (quota→network re-announces; clear resets; no mount announce). Blank-online prompt left silent (informational, not a failure swap). 0 diagnostics. kimi-review: no findings (resolved the kind-change WARNING inline)                                                                                                                                                                                                                                                                                                           |

### Low

| ID  | Finding                                                                                                                                                                                                                                                                                                                             | Domain         | Agent                    | File(s)                                                                                | Research   | Status   | Verification                                                                                                                                                                                                                            |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------ | -------------------------------------------------------------------------------------- | ---------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L1  | Pantry `unit`/`category` (free-text, `z.string().max(50)`) concatenated raw into the meal-plan LLM prompt; only `item.name` passes `sanitizeUserInput`. Self-injection only, bounded by SYSTEM_PROMPT_BOUNDARY + 50 chars. (Security agent auto-filed `todos/2026-05-20-sanitize-pantry-unit-category-prompt.md` in main checkout.) | security       | security-auditor         | `server/services/pantry-meal-plan.ts:144-146`                                          | —          | deferred | → `todos/2026-05-20-sanitize-pantry-unit-category-prompt.md`                                                                                                                                                                            |
| L2  | New TTL caches purge only lazily on next same-user read — no sweep / size bound; Map grows monotonically with distinct lifetime users.                                                                                                                                                                                              | performance    | performance-specialist   | `server/services/subscription-tier-cache.ts:27`, `verification-streak-cache.ts:17`     | —          | deferred | → `todos/2026-05-20-ttl-cache-eviction-and-invalidation.md`                                                                                                                                                                             |
| L3  | Same two caches expose no `invalidateCache()` for mutation-time eviction — stale tier/streak for up to the 60s TTL after an IAP upgrade/downgrade.                                                                                                                                                                                  | performance    | performance-specialist   | `server/services/subscription-tier-cache.ts`, `verification-streak-cache.ts`           | —          | deferred | → `todos/2026-05-20-ttl-cache-eviction-and-invalidation.md` (same todo as L2)                                                                                                                                                           |
| L4  | Allergen backfill issues one `db.update()` per row (rule #19 anti-pattern); one-shot script, not a hot path.                                                                                                                                                                                                                        | data-integrity | database-specialist      | `server/scripts/backfill-recipe-allergens.ts:48-52,76-80`                              | —          | deferred | → `todos/2026-05-20-allergen-backfill-batch-update.md` (also re-derives the M1-stale cache)                                                                                                                                             |
| L5  | Storage modules over the 500-line split cap (rule 1): `community.ts` 563, `meal-plan-recipes.ts` 578. No new exported functions this window — only the line cap is breached.                                                                                                                                                        | architecture   | architecture-specialist  | `server/storage/community.ts`, `meal-plan-recipes.ts`                                  | —          | deferred | → `todos/2026-05-20-split-oversized-storage-modules.md`                                                                                                                                                                                 |
| L6  | Receipt hooks return `response.json()` cast to result type with no Zod validation, and read `errorData.error` off `any`. The 2026-05-16 Zod-validation pass covered only recipe-search/catalog hooks.                                                                                                                               | code-quality   | quality-specialist       | `client/hooks/useReceiptScan.ts:72,78,103,106`                                         | better-fix | deferred | → `todos/2026-05-20-receipt-hooks-zod-response-validation.md` (Zod schema + `.safeParse()` at boundary; type errorData)                                                                                                                 |
| L7  | `User.subscriptionTier?: "free" \| "premium"` re-inlines the union that `SubscriptionTier` exports from `@shared/types/premium`; the `MeasurementUnit` import 2 lines above is the correct pattern.                                                                                                                                 | code-quality   | typescript-specialist    | `shared/types/auth.ts:14`                                                              | —          | deferred | → `todos/2026-05-20-subscription-tier-type-import-dedup.md`                                                                                                                                                                             |
| L8  | `useLogWeight` defines only `onSuccess`; failed POST /api/weight gives no user feedback (no Alert/InlineError, success haptic silently absent) — inconsistent with the screen's existing InlineError UX.                                                                                                                            | code-quality   | quality-specialist       | `client/hooks/useWeightLogs.ts:29-50`, `client/screens/WeightTrackingScreen.tsx:87-99` | —          | verified | `onError` added at both `.mutate()` call sites (log + goal): sets InlineError + error haptic + iOS announce (log). Hook unchanged (error handled at call site). 0 diagnostics. kimi WARNING (no test for onError path) → Deferred Items |
| L9  | `aiResponseSchema.parse(parsed)` shape-mismatch (valid JSON, wrong shape) throws raw ZodError → 500 (by design), but has no test; suite covers empty-content and invalid-JSON only.                                                                                                                                                 | code-quality   | testing-specialist       | `server/services/pantry-meal-plan.ts:250`                                              | —          | deferred | → `todos/2026-05-20-pantry-meal-plan-schema-mismatch-test.md`                                                                                                                                                                           |
| L10 | `usePhotoAnalysis` abort-on-blur: now the AbortSignal actually fires, an OS-level focus blur mid-upload aborts → catch returns early without `error`, finally skips `setIsAnalyzing(false)` (signal.aborted), no restart on refocus → spinner can strand. Thin reachability (no in-app nav during spinner).                         | camera         | camera-specialist        | `client/hooks/usePhotoAnalysis.ts:100-117,168-188`                                     | confirmed  | verified | `finally` now always `setIsAnalyzing(false)` (dropped `!signal.aborted` guard); no-op on unmount, clears stranded spinner on OS-blur refocus. kimi-review: no findings                                                                  |
| L11 | "Save goal weight" button has `disabled={!goalInput}` but no `accessibilityState={{ disabled }}`; the Log-weight button above sets it. TalkBack ignores `disabled` alone.                                                                                                                                                           | accessibility  | accessibility-specialist | `client/screens/WeightTrackingScreen.tsx:357-371`                                      | confirmed  | verified | `accessibilityState={{ disabled: !goalInput }}` added (matches Log-weight button). 0 diagnostics                                                                                                                                        |
| L12 | Icon-only `Feather` glyphs inside labeled Pressables lack `accessible={false}` (plus :202, check :370).                                                                                                                                                                                                                             | accessibility  | accessibility-specialist | `client/screens/WeightTrackingScreen.tsx:202,370`                                      | confirmed  | verified | `accessible={false}` on both Feather glyphs (plus/check) inside labeled Pressables. 0 diagnostics                                                                                                                                       |

## Deferred Items

Findings deferred at Phase 2.5 triage get a todo. Surfaced WARNING/MEDIUM/LOW items from the fix/review phases stay here as a record (no auto-todo) — user decides at close.

| ID    | Todo / Source                                               | Rationale                                                                                                                                                                                                                                                                                                                         |
| ----- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1-w  | kimi WARNING (Phase 3, M1)                                  | Multi-token plant qualifiers ("soy-based milk", "oat-based flour") still flag dairy/wheat. SAFE over-flag direction (no under-flag); rare phrasing vs "soy milk"/"almond flour". Expanding the regex to arbitrary infixes risks dangerous over-suppression — not worth it. Common forms fixed + tested.                           |
| L8-w  | kimi WARNING (Phase 3, L8)                                  | No render-test for the new `onError` paths on `logWeight`/`setGoalWeight` (WeightTrackingScreen). Screen has no existing test harness; a full jsdom render + failing-mutation mock is disproportionate to a Low a11y/UX fix. Handlers are simple (set error state + haptic + announce). User decides if a test todo is warranted. |
| L1    | `todos/2026-05-20-sanitize-pantry-unit-category-prompt.md`  | Low; self-injection bounded by 50-char limits + SYSTEM_PROMPT_BOUNDARY. Isolated security file (no Medium touches it).                                                                                                                                                                                                            |
| L2+L3 | `todos/2026-05-20-ttl-cache-eviction-and-invalidation.md`   | Low; no prod deployment, process restart resets the Maps. Bound + invalidate before scale.                                                                                                                                                                                                                                        |
| L4    | `todos/2026-05-20-allergen-backfill-batch-update.md`        | Low; one-shot script, not a hot path. Tied to M1 — re-running it refreshes the M1-stale cache.                                                                                                                                                                                                                                    |
| L5    | `todos/2026-05-20-split-oversized-storage-modules.md`       | Low; line-cap only (no new functions added this window). Mechanical split, deferred to keep audit diff focused.                                                                                                                                                                                                                   |
| L6    | `todos/2026-05-20-receipt-hooks-zod-response-validation.md` | Low; isolated code-quality file. Research verdict better-fix (Zod `.safeParse()` at boundary).                                                                                                                                                                                                                                    |
| L7    | `todos/2026-05-20-subscription-tier-type-import-dedup.md`   | Low; DRY/drift only. Needs a `subscriptionTiers`-width check before the swap.                                                                                                                                                                                                                                                     |
| L9    | `todos/2026-05-20-pantry-meal-plan-schema-mismatch-test.md` | Low; behavior is correct (500 by design), only a test-coverage gap.                                                                                                                                                                                                                                                               |

## Summary

| Severity  | Found | Verified | Deferred | False-positive | Open  |
| --------- | ----- | -------- | -------- | -------------- | ----- |
| Critical  | 0     | 0        | 0        | 0              | 0     |
| High      | 0     | 0        | 0        | 0              | 0     |
| Medium    | 5     | 5        | 0        | 0              | 0     |
| Low       | 12    | 4        | 8        | 0              | 0     |
| **Total** | 17    | 9        | 8        | 0              | **0** |

## Fix Commits

| Commit | Description |
| ------ | ----------- |
| —      | —           |

## Codification (Phase 8)

### Solutions Extracted

| Finding | Category | Added To |
| ------- | -------- | -------- |
| —       | —        | —        |

### Rules Updates

| Finding | Rules File | New Rule Added |
| ------- | ---------- | -------------- |
| —       | —          | —              |

### Code Reviewer Updates

| Finding | New Check Added |
| ------- | --------------- |
| —       | —               |

### Specialist Agent Updates

| Finding | Agent Updated | New Check Added |
| ------- | ------------- | --------------- |
| —       | —             | —               |

## Post-Audit Notes

**Phase 5 close verification:**

- `npm run check:types` — 0 errors (project-wide).
- `npx eslint` on all 6 changed files — clean (one Prettier wrap auto-fixed in `allergens.test.ts`).
- Tests exercising the changed code: `allergens.test.ts` 51/51, `recipe-search.test.ts` 50/50 (the `safeForMe` predicate consumes the M1 change). `allergens` is the only test file importing the allergen functions directly; `recipe-search` is the main indirect consumer — both green.
- Full `test:run` (5,000+) NOT run locally — per CLAUDE.md, CI owns the full gate on push. Changes are localized: 1 shared util (comprehensively tested), 4 client screens/hooks (IDE-diagnostics + lint clean, no existing test files).

**Process notes:**

- Baseline taken from CI (green at `6b833ad1`) rather than a local run, per CLAUDE.md.
- Repo advanced mid-session: the user committed `d7cdebd9` (todo-archive) in a parallel terminal after session start; the worktree was created from the new HEAD.
- The `security-auditor` discovery agent auto-filed an L1 todo in the **main checkout** (outside the worktree). Removed the stray and recreated it inside the worktree so it ships with the audit commit — avoids an untracked-file merge collision on integration.
- kimi-review (per fix-unit) surfaced 3 WARNINGs: M1 multi-token qualifier (deferred, safe direction), L8 onError test-coverage (deferred), M5 error-kind re-announce (fixed inline). No CRITICALs.

**Phase 6 code-review (code-reviewer subagent):** 0 CRITICAL, 0 HIGH. PASS on all safety-critical items — animal-milk qualifiers (goat/sheep/buffalo/camel) and gluten grains (spelt/rye/barley) confirmed NOT suppressed (no under-flag path); substitutes' own allergens still fire; sessionId narrowing + spinner fix correct. Two items fixed inline: (LOW) `setGoalWeight` onError now also calls `announceForAccessibility` for Android parity with the log path; (MEDIUM) added an ingredient-text-semantics rationale comment for the GF-flour suppression in `allergens.ts`. Re-verified: `check:types` 0 errors, lint clean.
