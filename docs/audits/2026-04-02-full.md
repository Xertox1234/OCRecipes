# Audit: Full Codebase Audit

> **Date:** 2026-04-02
> **Trigger:** Periodic full codebase audit
> **Domains:** security, performance, data-integrity, architecture, code-quality
> **Baseline:** 3228/3243 tests passing (227/228 files) | 1 type error | 0 lint errors (11 warnings)
> **Pre-existing failures:** chat.test.ts (15 tests — `type` column missing from `chat_conversations`), useCollapsibleHeight.test.ts type error
> **Raw agent findings:** 38 across 5 domains → 35 after dedup
> **Post-fix:** 3228/3243 tests passing (227/228 files) | 1 type error | 0 lint errors (8 warnings — 3 eliminated)

## Findings

Each finding has a lifecycle: `open` → `fixing` → `verified` or `deferred` or `false-positive`.

**Status key:**

- `open` — Found but not yet addressed
- `fixing` — Work in progress
- `verified` — Fix applied AND confirmed by test/grep/type-check
- `deferred` — Intentionally postponed (must link to todo)
- `false-positive` — Agent was wrong or issue was already fixed

### Critical

| ID  | Finding                                                                                       | Domain      | File(s)                | Status   | Verification                                                                     |
| --- | --------------------------------------------------------------------------------------------- | ----------- | ---------------------- | -------- | -------------------------------------------------------------------------------- |
| C1  | Missing index on `community_recipes.is_public` — full table scan on carousel/featured/profile | performance | `shared/schema.ts:493` | verified | grep confirms `isPublicCreatedAtIdx` composite index added; 3228/3243 tests pass |

### High

| ID  | Finding                                                                        | Domain         | File(s)                                                                              | Status   | Verification                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------ | -------------- | ------------------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | `updateUser` accepts `Partial<User>` — allows overwriting dangerous fields     | data-integrity | `server/storage/users.ts:86-96`                                                      | verified | `UpdatableUserFields` pick type created; `updateUser` parameter changed; 3228 tests pass                                                                          |
| H2  | `apiKeys.keyPrefix` has no unique constraint — collision at ~18K keys          | data-integrity | `shared/schema.ts:1657`                                                              | verified | `prefixIdx` changed from `index` to `uniqueIndex`; grep confirms                                                                                                  |
| H3  | `getRecentCommunityRecipes` over-fetches up to 508 rows then filters in JS     | performance    | `server/storage/carousel.ts:103-143`                                                 | verified | Dismissed IDs pushed into SQL via `notInArray`; dead diet filter removed; limit applied in query                                                                  |
| H4  | Services bypass storage with direct DB imports                                 | architecture   | `server/services/nutrition-lookup.ts`, `server/services/meal-type-inference.ts`      | verified | Cache functions moved to `storage/cache.ts`; backfill operations moved to `storage/meal-plans.ts`; `db` import removed from services                              |
| H5  | Storage imports from service layer (layering inversion)                        | architecture   | `server/storage/sessions.ts:14`, `server/storage/chat.ts:13`                         | verified | `AnalysisResult` moved to `shared/types/photo-analysis.ts`; `recipeChatMetadataSchema` moved to `shared/schemas/recipe-chat.ts`; storage imports from shared only |
| H6  | Duplicated Zod schemas across recipe-chat, recipe-generation, meal-suggestions | code-quality   | `server/services/recipe-chat.ts:54-98`, `server/services/recipe-generation.ts:30-77` | verified | Agent consolidated shared schemas; grep confirms dedup                                                                                                            |

### Medium

| ID  | Finding                                                              | Domain         | File(s)                                                                       | Status   | Verification                                                                                          |
| --- | -------------------------------------------------------------------- | -------------- | ----------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------- |
| M1  | Unsanitized `question` in AI prompt (`refineAnalysis`)               | security       | `server/services/photo-analysis.ts`                                           | verified | `sanitizeUserInput(question)` added; grep confirms `sanitizedQuestion` used in template               |
| M2  | API key cache not invalidated on revoke/tier-change                  | security       | `server/routes/admin-api-keys.ts`                                             | verified | `clearApiKeyCache()` called after revoke and tier update; grep confirms                               |
| M3  | No admin operation audit logging + whitespace in ADMIN_USER_IDS      | security       | `server/routes/admin-api-keys.ts`, `server/routes/_helpers.ts`                | verified | `.map(s => s.trim())` added; `logger.info` audit logging added for create/revoke/tier-update          |
| M4  | `getLibraryCounts` — global `featuredRecipes` count not cached       | performance    | `server/storage/profile-hub.ts`                                               | verified | In-memory cache with 5-min TTL for featuredRecipes count; per-user counts unchanged                   |
| M5  | `scanHistory` count includes soft-deleted items                      | data-integrity | `server/storage/profile-hub.ts:16`                                            | verified | `AND discarded_at IS NULL` added to subselect; grep confirms                                          |
| M6  | Goal calculation writes 2 tables without transaction                 | data-integrity | `server/routes/goals.ts`                                                      | verified | New `updateUserGoalsAndProfile()` atomic storage function; goals route updated; goals tests pass      |
| M7  | `likeCount` never incremented — dead column misleads ordering        | data-integrity | `server/storage/community.ts`                                                 | verified | `likeCount` removed from ORDER BY; ordering now `createdAt DESC` only                                 |
| M8  | `reformulationFlags` duplicate active flags per barcode              | data-integrity | `shared/schema.ts`                                                            | verified | Partial unique index `reformulation_flags_active_unique` added on `barcode WHERE status = 'flagged'`  |
| M9  | Verification submit — 4-5 sequential DB reads parallelizable         | performance    | `server/routes/verification.ts`                                               | verified | Independent reads wrapped in `Promise.all`                                                            |
| M10 | `RecipeChatScreen` FlatList missing `FLATLIST_DEFAULTS`              | performance    | `client/screens/RecipeChatScreen.tsx`                                         | verified | `FLATLIST_DEFAULTS` spread added to FlatList; import confirmed                                        |
| M11 | Cached coach response — artificial 1s+ delay                         | performance    | `server/routes/chat.ts`                                                       | verified | Word-by-word streaming replaced with 3 chunks at 15ms delay (~30ms total)                             |
| M12 | Session stores scattered in route files                              | architecture   | `server/routes/cooking.ts`, `server/routes/verification.ts`                   | verified | Stores moved to `server/storage/sessions.ts`; routes import from storage                              |
| M13 | `_helpers.ts` god module (449 lines, 40+ exports)                    | architecture   | `server/routes/_helpers.ts`                                                   | deferred | `todos/split-helpers-god-module.md`                                                                   |
| M14 | 12 route files use manual catch blocks instead of `handleRouteError` | architecture   | 12 route files                                                                | verified | All 12 files updated to use `handleRouteError`; unused logger imports removed                         |
| M15 | Triplicated `findNutrient` helper                                    | code-quality   | `server/services/nutrition-lookup.ts`                                         | verified | Generic `findNutrientValue<T>()` extracted; 3 closures now delegate to it                             |
| M16 | Duplicated `buildDietaryContext` across 3 services                   | code-quality   | `server/services/recipe-generation.ts`, `server/services/meal-suggestions.ts` | deferred | `todos/consolidate-build-dietary-context.md`                                                          |
| M17 | `allergies as { name: string }[]` cast repeated 4x                   | code-quality   | 3 service files                                                               | verified | All 4 sites replaced with `parseUserAllergies()`; tests updated with severity field; 31/31 tests pass |

### Low

| ID  | Finding                                               | Domain         | File(s)                                       | Status   | Verification                                                                                                       |
| --- | ----------------------------------------------------- | -------------- | --------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------ | ------------------- | ----- |
| L1  | Unvalidated `intentRaw` cast in photo analysis        | security       | `server/routes/photos.ts`                     | verified | Replaced unsafe cast with `z.enum([...]).catch("log")` validation                                                  |
| L2  | `/assets` static mount lacks `dotfiles: "deny"`       | security       | `server/index.ts`                             | verified | `{ dotfiles: "deny" }` added to express.static options                                                             |
| L3  | Predictable restore transaction IDs                   | security       | `server/routes/subscription.ts`               | verified | Replaced `Date.now()-userId` with `crypto.randomUUID()`                                                            |
| L4  | No magic-byte validation on audio uploads             | security       | `server/routes/food.ts`                       | verified | `detectAudioMimeType()` created in `server/lib/audio-mime.ts`; check added before Whisper API; 9/9 food tests pass |
| L5  | `coachResponseCache` not in periodic cache cleanup    | data-integrity | `server/storage/cache.ts`                     | verified | `coachResponseCache` added to `purgeExpiredCacheRows` tables array                                                 |
| L6  | `medicationLogs.appetiteLevel` lacks CHECK constraint | data-integrity | `shared/schema.ts`                            | verified | `appetiteRange` CHECK constraint added (1-5)                                                                       |
| L7  | `fastingSchedules` boolean columns nullable           | data-integrity | `shared/schema.ts`                            | verified | `.notNull()` added to `isActive`, `notifyEatingWindow`, `notifyMilestones`, `notifyCheckIns`                       |
| L8  | `MiniWidgetRow` re-renders all widgets every 60s      | performance    | `client/components/profile/MiniWidgetRow.tsx` | verified | 60s interval moved into memoized `FastingWidget`; parent no longer ticks                                           |
| L9  | `SuggestedPrompts` not memoized                       | performance    | `client/screens/ChatScreen.tsx`               | verified | Wrapped with `React.memo`                                                                                          |
| L10 | Duplicated empty label result objects                 | code-quality   | `server/services/photo-analysis.ts`           | verified | `EMPTY_LABEL_RESULT` constant extracted; 2 duplicate objects replaced with `{ ...EMPTY_LABEL_RESULT }`             |
| L11 | `unknown` types in `ProfileWidgetData.fasting`        | code-quality   | `server/services/profile-hub.ts`              | verified | Replaced `unknown` with `FastingSchedule                                                                           | null`and`FastingLog | null` |

## Deferred Items

| ID  | Todo                                         | Rationale                                                                                                                                    |
| --- | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| M13 | `todos/split-helpers-god-module.md`          | Large refactoring (split 449-line file, update 24 importers) with no production risk — high merge conflict risk if done alongside other work |
| M16 | `todos/consolidate-build-dietary-context.md` | Moderate effort refactoring — 3 function variants with subtle differences require careful unification                                        |

## Summary

| Severity  | Found | Verified | Deferred | False-positive | Open  |
| --------- | ----- | -------- | -------- | -------------- | ----- |
| Critical  | 1     | 1        | 0        | 0              | 0     |
| High      | 6     | 6        | 0        | 0              | 0     |
| Medium    | 17    | 15       | 2        | 0              | 0     |
| Low       | 11    | 11       | 0        | 0              | 0     |
| **Total** | 35    | 33       | 2        | 0              | **0** |

## Fix Commits

| Commit    | Description                                                |
| --------- | ---------------------------------------------------------- |
| `cbbd92f` | fix: resolve full audit findings (33 verified, 2 deferred) |

## Codification (Phase 7)

Completed after fixes are committed. Each row links to the docs change.

### Patterns Extracted

| Finding | Pattern                                                    | Added To                    |
| ------- | ---------------------------------------------------------- | --------------------------- |
| L4      | Magic-byte validation for all file uploads (audio + image) | `docs/patterns/security.md` |
| M2      | Admin operations must invalidate in-memory caches          | `docs/patterns/database.md` |

### Learnings Extracted

| Finding | Learning Title                                               | Category |
| ------- | ------------------------------------------------------------ | -------- |
| M1      | Unsanitized AI prompt parameter that looked server-generated | Security |

### Code Reviewer Updates

| Finding | New Check Added                                          |
| ------- | -------------------------------------------------------- |
| H4      | Services must not import `db` directly                   |
| H5      | Storage must not import from services                    |
| H1      | Update functions must use pick-type whitelists           |
| M2      | Admin ops must invalidate caches                         |
| M5      | Soft-delete filter on new queries (recurring regression) |
| M12     | Session stores must live in storage layer                |
| M14     | Route catch blocks must use `handleRouteError`           |
| L4      | File uploads need magic-byte validation                  |

**Codification commit:** `a5528a6`

## Post-Audit Notes

- 5th full audit. Agent quality continues improving — fewer false positives than earlier rounds.
- Architecture agent found the deepest issues (layering violations, scattered session stores) that other agents missed.
- Code quality agent's duplication findings (H6, M15-M17) had the highest fix-to-effort ratio.
- 6 archived todos from previous audits were resolved by this audit's fixes.
