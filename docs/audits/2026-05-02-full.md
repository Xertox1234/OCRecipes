# Audit: Full Codebase — 2026-05-02

> **Date:** 2026-05-02
> **Trigger:** Periodic full audit — ~20 commits since 2026-04-28: QuickLog inline drawer feature (useQuickLogSession hook extraction, QuickLogDrawer component, Home inline rendering, ScanScreen confirm overlay / returnAfterLog), ChatStackNavigator deferred mount fix
> **Domains:** security, performance, data-integrity, architecture, code-quality, camera/RN-UX
> **Baseline:** 4244 tests passing (305 files) | 0 type errors | 4 lint warnings, 0 errors

## Findings

Each finding has a lifecycle: `open` → `fixing` → `verified` or `deferred` or `false-positive`.

**Status key:**

- `open` — Found but not yet addressed
- `fixing` — Work in progress
- `verified` — Fix applied AND confirmed by test/grep/type-check
- `deferred` — Intentionally postponed (must link to todo)
- `false-positive` — Agent was wrong or issue was already fixed

### Critical

| ID  | Finding                                                                                                                                                                                                                                    | Domain | Agent             | File(s)                                 | Status   | Verification                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ----------------- | --------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------- |
| C1  | `handleConfirmDismiss` dispatches `RESET` but never resets `hasLockedRef.current = false` or dispatches `CAMERA_READY`; `isFocused` effect only fires on focus-change so ref is never cleared — scan permanently blocked after one Dismiss | camera | camera-specialist | `client/screens/ScanScreen.tsx:244-247` | verified | `hasLockedRef.current = false; dispatch({ type: "CAMERA_READY" })` added; 53/53 ScanScreen tests pass |

### High

| ID  | Finding                                                                                                                                                                                        | Domain      | Agent               | File(s)                                             | Status   | Verification                                                                                     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ------------------- | --------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| H1  | `submitLog` `useCallback` lists entire `logAllMutation` object as dep (new reference every render); only destructured `mutate`/`isPending` are stable per TanStack Query v5 pattern            | performance | database-specialist | `client/hooks/useQuickLogSession.ts:172`            | verified | `logAllMutate` destructured; 8/8 useQuickLogSession tests pass                                   |
| H2  | `handleToggle` `useCallback` lists entire `session` return object as dep (plain object literal, always a new reference); only `session.reset` is actually used                                 | performance | database-specialist | `client/components/home/QuickLogDrawer.tsx:83`      | verified | `sessionReset` destructured from `session`; 78/78 home component tests pass                      |
| H3  | `confirmOverlay` style uses `paddingBottom: Spacing.xl` (20 px hardcoded); `insets.bottom` (34 px on notched devices) not applied — "Log it" button obscured by home indicator                 | camera      | camera-specialist   | `client/screens/ScanScreen.tsx:788-796`             | verified | `paddingBottom: insets.bottom + Spacing.lg` added inline; static style removed; 53/53 tests pass |
| H4  | `CameraView isActive={isFocused}` keeps camera hardware running while confirm overlay is visible; `hasLockedRef` prevents re-lock but scan callback still fires every frame and wastes battery | camera      | camera-specialist   | `client/screens/ScanScreen.tsx:448`                 | verified | `isActive={isFocused && !confirmCard}`; 53/53 tests pass                                         |
| H5  | `accessibilityLiveRegion="polite"` on `parseError`/`submitError` nodes in QuickLogDrawer is Android-only; no paired `AccessibilityInfo.announceForAccessibility()` call for iOS VoiceOver      | camera      | camera-specialist   | `client/components/home/QuickLogDrawer.tsx:202,315` | verified | iOS `announceForAccessibility` effects added for both errors; 78/78 tests pass                   |

### Medium

| ID  | Finding                                                                                                                                                                                                                           | Domain         | Agent               | File(s)                                                           | Status   | Verification                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------- | ----------------------------------------------------------------- | -------- | ------------------------------------------------------------- |
| M1  | `handleConfirmLog` POSTs to `/api/scanned-items` but never calls `queryClient.invalidateQueries` for `dailySummary` or `scannedItems`; Home nutrition ring stays stale after a confirm-overlay log                                | architecture   | database-specialist | `client/screens/ScanScreen.tsx:223-242`                           | deferred | `todos/2026-05-02-scan-confirm-query-invalidation.md`         |
| M2  | `returnAfterLog` fetch is a bare `.then()/.catch()` chain with no `AbortController` cleanup; if user taps Dismiss while fetch is in-flight, `setConfirmCard(...)` fires on a reset screen re-showing overlay                      | data-integrity | database-specialist | `client/screens/ScanScreen.tsx:188-207`                           | deferred | `todos/2026-05-02-scan-confirm-abort-controller.md`           |
| M3  | `logAllMutation.mutationFn` uses `Promise.all`; if one POST fails, `onError` leaves all items in `parsedItems` — retry re-submits already-persisted items, creating duplicate `daily_logs` entries (no DB-level uniqueness guard) | data-integrity | database-specialist | `client/hooks/useQuickLogSession.ts:113-151`                      | deferred | `todos/2026-05-02-quick-log-partial-failure-retry.md`         |
| M4  | `sendDue*Reminders` functions use check-then-insert pattern across two separate queries with no `UNIQUE` constraint on `(userId, type, scheduledFor::date)` — concurrent scheduler runs insert duplicate pending reminders        | data-integrity | database-specialist | `server/services/notification-scheduler.ts:83-97,179-192,242-253` | deferred | `todos/2026-05-02-reminder-scheduler-unique-constraint.md`    |
| M5  | All three `/api/reminders/*` endpoints (`GET pending`, `POST acknowledge`, `PATCH mutes`) use only `requireAuth` with no rate limiter; `POST acknowledge` performs a bulk UPDATE + returns `coachContext` data                    | security       | security-auditor    | `server/routes/reminders.ts:20-75`                                | deferred | `todos/2026-05-02-reminders-route-rate-limit.md`              |
| M6  | `totalCalories` (`.reduce()`) and `hasParsedItems` computed inline on every render of `QuickLogDrawer`, including high-frequency `volume` re-renders during speech input; not wrapped in `useMemo`                                | performance    | database-specialist | `client/components/home/QuickLogDrawer.tsx:102-106`               | deferred | `todos/2026-05-02-quick-log-drawer-memoize-computed.md`       |
| M7  | `Animated.ScrollView` on `HomeScreen` has no `keyboardShouldPersistTaps` prop (defaults to `"never"`); first chip tap while keyboard is open is consumed by scroll view to dismiss keyboard — chip `onPress` never fires          | camera         | camera-specialist   | `client/screens/HomeScreen.tsx:132-151`                           | deferred | `todos/2026-05-02-home-scroll-keyboard-persist-taps.md`       |
| M8  | `ChatStackNavigator` returns `null` while `isPremiumLoading` is `true` — Coach tab is completely blank (no spinner, no skeleton) on every app cold-start until premium queries resolve                                            | camera         | camera-specialist   | `client/navigation/ChatStackNavigator.tsx:26`                     | deferred | `todos/2026-05-02-chat-navigator-premium-loading-skeleton.md` |
| M9  | No test coverage for the `returnAfterLog`/`confirmCard` flow: overlay setup, product fetch, Log It / Dismiss interactions, error recovery — all new async state transitions are untested                                          | code-quality   | testing-specialist  | `client/screens/__tests__/` (missing)                             | deferred | `todos/2026-05-02-scan-confirm-overlay-tests.md`              |

### Low

| ID  | Finding                                                                                                                                                                                                                                           | Domain         | Agent               | File(s)                                                    | Status   | Verification                                                                                                    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------- | ---------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------- |
| L1  | `barcode` field in `scannedItemInputSchema` has no format validation (no digit-only regex, no length cap); `GET /api/nutrition/barcode/:code` correctly enforces `^\d+$` and length ≤50 at line 92                                                | security       | security-auditor    | `server/routes/nutrition.ts:28-42`                         | deferred | `todos/2026-05-02-barcode-input-schema-validation.md`                                                           |
| L2  | `productName` in `scannedItemInputSchema` has `.min(1)` but no `.max()` — arbitrary-length strings accepted and stored; comparable fields in other schemas have `.max()` caps                                                                     | security       | security-auditor    | `server/routes/nutrition.ts:29-32`                         | deferred | `todos/2026-05-02-barcode-input-schema-validation.md`                                                           |
| L3  | When barcode lookup fails, `.catch()` sets `name: "Food item"`, `calories: null`, then user can still tap Log It — stores a barcode-linked row with no nutrition data, inflating item count without contributing to `totalCalories`               | data-integrity | database-specialist | `client/screens/ScanScreen.tsx:199-207`                    | deferred | `todos/2026-05-02-scan-confirm-null-calories-guard.md`                                                          |
| L4  | `handleTextSubmit` calls `parseFoodTextMutate` unconditionally with no `isParsing` guard; if voice auto-parse fires concurrently (isFinal + Return key), two in-flight parses race — last `onSuccess` silently drops the first set of items       | data-integrity | database-specialist | `client/hooks/useQuickLogSession.ts:76-90`                 | deferred | `todos/2026-05-02-quick-log-concurrent-parse-guard.md`                                                          |
| L5  | `logAllMutation.mutationFn` always sets `sourceType: "voice"` regardless of whether items came from voice, typed text, or chip tap — corrupts provenance analytics                                                                                | code-quality   | testing-specialist  | `client/hooks/useQuickLogSession.ts:119`                   | deferred | `todos/2026-05-02-quick-log-source-type-provenance.md`                                                          |
| L6  | `res.json()` in `logAllMutation.mutationFn` returns untyped `Promise<any>`; `_data` in `onSuccess` is `any[]` — breaks type safety on server response                                                                                             | code-quality   | testing-specialist  | `client/hooks/useQuickLogSession.ts:126`                   | deferred | `todos/2026-05-02-quick-log-mutation-response-type.md`                                                          |
| L7  | `onLogSuccess` passed as inline arrow prop to `useQuickLogSession` — new reference every render; the hook's ref-mirror guard is correct but the sync `useEffect` fires on every high-frequency `volume` render                                    | performance    | database-specialist | `client/components/home/QuickLogDrawer.tsx:58-64`          | deferred | `todos/2026-05-02-quick-log-drawer-inline-arrow-perf.md`                                                        |
| L8  | Inline arrow closures in parsed-items `.map()` at lines 216 and 268 — `session.handleChipPress` and `session.removeItem` are stable callbacks but wrapped inline, creating new references on every render                                         | performance    | database-specialist | `client/components/home/QuickLogDrawer.tsx:216,268`        | deferred | `todos/2026-05-02-quick-log-drawer-inline-arrow-perf.md`                                                        |
| L9  | `contentContainerStyle` inline object literal + `onRefresh` inline arrow in `HomeScreen` recreated on every scroll event (16ms throttle); both are stable-dep candidates for `useMemo`/`useCallback`                                              | performance    | database-specialist | `client/screens/HomeScreen.tsx:134-149`                    | deferred | `todos/2026-05-02-quick-log-drawer-inline-arrow-perf.md`                                                        |
| L10 | `PremiumContext.Provider` passes inline `value={{ tier, features, ... }}` — new object reference on every render; newly exercised by ChatStackNavigator's `usePremiumContext()` call                                                              | performance    | database-specialist | `client/context/PremiumContext.tsx:152-175`                | deferred | `todos/2026-05-02-premium-context-stable-value.md`                                                              |
| L11 | `frequentItems` query fires unconditionally on mount of `useQuickLogSession` (called inside always-mounted `QuickLogDrawer`) — fetches on every `HomeScreen` cold load even if drawer is never opened; `enabled: isOpen` would defer until needed | performance    | database-specialist | `client/hooks/useQuickLogSession.ts:154-166`               | deferred | `todos/2026-05-02-quick-log-session-defer-frequent-items.md`                                                    |
| L12 | `logAllMutation.mutationFn` fires N parallel POSTs with no client-side item count cap; typical voice parse yields 3–5 items but crafted input can produce ~10–15, bypassing rate-limiting-per-item intent                                         | security       | security-auditor    | `client/hooks/useQuickLogSession.ts:114-128`               | deferred | `todos/2026-05-02-quick-log-item-count-cap.md`                                                                  |
| L13 | `handleConfirmDismiss` `Pressable` missing `accessibilityState={{ disabled: confirmCard.isLogging }}`; "Log it" Pressable correctly has `accessibilityState={{ busy: isLogging }}` — inconsistent                                                 | camera         | camera-specialist   | `client/screens/ScanScreen.tsx:646-664`                    | deferred | `todos/2026-05-02-scan-confirm-overlay-accessibility.md`                                                        |
| L14 | Confirm overlay loading state ("Identifying food…" + spinner) not announced to VoiceOver/TalkBack; no `accessibilityLiveRegion` or `AccessibilityInfo.announceForAccessibility()` when fetch starts/completes                                     | camera         | camera-specialist   | `client/screens/ScanScreen.tsx:612-620`                    | deferred | `todos/2026-05-02-scan-confirm-overlay-accessibility.md`                                                        |
| L15 | QuickLogDrawer chevron rotation: `useEffect` snaps `chevronRotation.value` on `reducedMotion` change without calling `cancelAnimation` first — ongoing `withTiming` may fight the snap                                                            | camera         | camera-specialist   | `client/components/home/QuickLogDrawer.tsx:91-96`          | verified | `isOpenRef` mirror + `cancelAnimation` added (matching WeightLogDrawer/FastingDrawer pattern); 78/78 tests pass |
| L16 | Mixed `React.useEffect(...)` (line 66) and destructured `useEffect(...)` (line 91) in same file — style inconsistency; `useEffect` is already imported at line 1                                                                                  | code-quality   | testing-specialist  | `client/components/home/QuickLogDrawer.tsx:66,91`          | deferred | `todos/2026-05-02-quick-log-drawer-react-useeffect-consistency.md`                                              |
| L17 | `handleVoicePress` stop-listening branch (`isListening: true` → calls `stopListening`) has no test; mocked `stopListening` is never asserted against                                                                                              | code-quality   | testing-specialist  | `client/hooks/__tests__/useQuickLogSession.test.ts`        | deferred | `todos/2026-05-02-quick-log-session-test-gaps.md`                                                               |
| L18 | `QuickLogDrawer` missing tests for: `speechError` toast, `submitError` display, camera button navigation, `isSubmitting` ActivityIndicator state                                                                                                  | code-quality   | testing-specialist  | `client/components/home/__tests__/QuickLogDrawer.test.tsx` | deferred | `todos/2026-05-02-quick-log-session-test-gaps.md`                                                               |

## Deferred Items

Items marked `deferred` must have a linked todo and rationale.

| ID      | Todo                                                               | Rationale                                                                    |
| ------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| M1      | `todos/2026-05-02-scan-confirm-query-invalidation.md`              | Low-urgency stale cache — ring self-heals on next navigate/refocus           |
| M2      | `todos/2026-05-02-scan-confirm-abort-controller.md`                | Edge case race; requires verifying apiRequest signal passthrough             |
| M3      | `todos/2026-05-02-quick-log-partial-failure-retry.md`              | Uncommon partial failure path; needs design decision on server vs client fix |
| M4      | `todos/2026-05-02-reminder-scheduler-unique-constraint.md`         | Requires DB migration; scheduler is single-process today                     |
| M5      | `todos/2026-05-02-reminders-route-rate-limit.md`                   | Rate limiting pattern is established; straightforward to add                 |
| M6      | `todos/2026-05-02-quick-log-drawer-memoize-computed.md`            | Perf optimization; not a correctness issue                                   |
| M7      | `todos/2026-05-02-home-scroll-keyboard-persist-taps.md`            | UX polish; second tap still works                                            |
| M8      | `todos/2026-05-02-chat-navigator-premium-loading-skeleton.md`      | Loading flash; not a crash or data issue                                     |
| M9      | `todos/2026-05-02-scan-confirm-overlay-tests.md`                   | New test coverage; component is complex to harness                           |
| L1+L2   | `todos/2026-05-02-barcode-input-schema-validation.md`              | Additive validation; low risk of exploit                                     |
| L3      | `todos/2026-05-02-scan-confirm-null-calories-guard.md`             | Product decision needed                                                      |
| L4      | `todos/2026-05-02-quick-log-concurrent-parse-guard.md`             | One-liner guard; very low priority                                           |
| L5      | `todos/2026-05-02-quick-log-source-type-provenance.md`             | Analytics only; no user impact                                               |
| L6      | `todos/2026-05-02-quick-log-mutation-response-type.md`             | Type safety; no runtime impact                                               |
| L7–L9   | `todos/2026-05-02-quick-log-drawer-inline-arrow-perf.md`           | Perf micro-optimization                                                      |
| L10     | `todos/2026-05-02-premium-context-stable-value.md`                 | Context optimization                                                         |
| L11     | `todos/2026-05-02-quick-log-session-defer-frequent-items.md`       | One extra query on cold load; cached after first fetch                       |
| L12     | `todos/2026-05-02-quick-log-item-count-cap.md`                     | Client-side rate limit defense                                               |
| L13+L14 | `todos/2026-05-02-scan-confirm-overlay-accessibility.md`           | Accessibility polish                                                         |
| L15     | _(fixed — see verified findings)_                                  | Elevated to Important by code reviewer; fixed immediately                    |
| L16     | `todos/2026-05-02-quick-log-drawer-react-useeffect-consistency.md` | Style consistency                                                            |
| L17+L18 | `todos/2026-05-02-quick-log-session-test-gaps.md`                  | Test coverage gaps                                                           |

## Summary

| Severity  | Found | Verified | Deferred | False-positive | Open  |
| --------- | ----- | -------- | -------- | -------------- | ----- |
| Critical  | 1     | 1        | 0        | 0              | 0     |
| High      | 5     | 5        | 0        | 0              | 0     |
| Medium    | 9     | 0        | 9        | 0              | 0     |
| Low       | 18    | 1        | 17       | 0              | 0     |
| **Total** | 33    | **7**    | **26**   | 0              | **0** |

## Fix Commits

| Commit | Description |
| ------ | ----------- |
| —      | —           |

## Codification (Phase 7)

Completed after fixes are committed.

### Patterns Extracted

| Finding | Pattern                                                                      | Added To                                                                               |
| ------- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| H4      | Camera `isActive` must include in-screen overlay state, not just `isFocused` | `docs/patterns/react-native.md` — "Camera `isActive`: Include In-Screen Overlay State" |

### Learnings Extracted

| Finding | Learning Title                                                                 | Category |
| ------- | ------------------------------------------------------------------------------ | -------- |
| C1      | `isFocused` Effect Dead Zone — In-Screen Overlays Don't Trigger Focus Re-Entry | Gotcha   |

### Code Reviewer Updates

| Finding | New Check Added |
| ------- | --------------- |
| —       | —               |

### Specialist Agent Updates

| Finding | Agent Updated                         | New Check Added                                                         |
| ------- | ------------------------------------- | ----------------------------------------------------------------------- |
| H4      | `.claude/agents/camera-specialist.md` | `isActive` must include overlay state                                   |
| C1      | `.claude/agents/camera-specialist.md` | In-session resets must be done imperatively, not via `isFocused` effect |

**Codification commit:** _(pending)_

## Post-Audit Notes

[Any process improvements or observations]
