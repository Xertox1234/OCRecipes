# Audit: Full Codebase Audit (Round 2)

> **Date:** 2026-04-07
> **Trigger:** Periodic full audit — significant new OCR feature landed since audit #6 (12 commits, ~2200 LOC)
> **Domains:** security, performance, data-integrity, architecture, code-quality, camera
> **Baseline:** 3258 tests passing (230 files) | 1 type error (pre-existing TS2347) | 0 lint errors
> **Focus:** New OCR frame processor feature (useOCRDetection, nutrition-ocr-parser, CameraView, ScanScreen, LabelAnalysisScreen)
> **Raw agent findings:** 43 across 6 domains → 28 after dedup + 3 false-positive
> **Post-fix:** 3286 tests passing (232 files) | 1 type error (pre-existing) | 0 lint errors (1 warning)

## Findings

Each finding has a lifecycle: `open` → `fixing` → `verified` or `deferred` or `false-positive`.

**Status key:**

- `open` — Found but not yet addressed
- `fixing` — Work in progress
- `verified` — Fix applied AND confirmed by test/grep/type-check
- `deferred` — Intentionally postponed (must link to todo)
- `false-positive` — Agent was wrong or issue was already fixed

### Critical

_None found._

### High

| ID  | Finding                                                                                                   | Domain      | Agent       | File(s)                                          | Status   | Verification                                                                                                                             |
| --- | --------------------------------------------------------------------------------------------------------- | ----------- | ----------- | ------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | `handleLog` useCallback depends on mutation objects (not destructured `.mutate`) — recreates every render | performance | Performance | `client/screens/LabelAnalysisScreen.tsx:297-303` | verified | Destructured `{ mutate: confirmLog, isPending: isConfirming }` and `{ mutate: verifyLog, isPending: isVerifying }`; 3286/3286 tests pass |
| H2  | HistoryScreen FlatList missing `FLATLIST_DEFAULTS` — highest-traffic screen lacks virtualization          | performance | Performance | `client/screens/HistoryScreen.tsx:726-791`       | verified | `{...FLATLIST_DEFAULTS}` spread added; grep confirms import and usage                                                                    |

### Medium

| ID  | Finding                                                                                              | Domain         | Agent                      | File(s)                                                | Status   | Verification                                                                                      |
| --- | ---------------------------------------------------------------------------------------------------- | -------------- | -------------------------- | ------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------- |
| M1  | `withRepeat` animation missing `cancelAnimation` on `reducedMotion` toggle                           | camera         | Camera, Performance        | `client/screens/ScanScreen.tsx:172-175`                | verified | `cancelAnimation(cornerOpacity)` added before static assignment; import added                     |
| M2  | Calories regex matches "Calories from Fat" before actual value — drops calories on pre-2020 labels   | data-integrity | Camera, Data Integrity     | `client/lib/nutrition-ocr-parser.ts:40`                | verified | Negative lookahead `(?!from\b)` added; new test "skips Calories from Fat" passes                  |
| M3  | Missing barcode Zod validation on front-label upload (uses raw `as string` cast)                     | security       | Security                   | `server/routes/verification.ts:316`                    | verified | `barcodeField.safeParse()` replaces raw cast; test updated; 24/24 verification tests pass         |
| M4  | Missing barcode format validation on label analysis upload                                           | security       | Security                   | `server/routes/photos.ts:539`                          | verified | Zod `z.string().regex(/^\d{8,14}$/)` validation added; invalid barcodes silently dropped          |
| M5  | OCR parser `extractNumber` accepts negative values (DB has CHECK >= 0 → 500 on confirm)              | data-integrity | Data Integrity             | `client/lib/nutrition-ocr-parser.ts:28-31`             | verified | `num < 0` rejection added; new test "rejects negative values" passes                              |
| M6  | `barcodeNutrition` table missing non-negative CHECK constraints (other nutrition tables have them)   | data-integrity | Data Integrity             | `shared/schema.ts:1686-1689`                           | verified | 4 CHECK constraints added (`bn_calories_gte0` etc.); follows `scannedItems` pattern               |
| M7  | No server-side validation of AI label nutrition values before storage (relies on DB CHECK for error) | data-integrity | Data Integrity             | `server/routes/photos.ts:596-622`                      | verified | `clamp()` function added; all nutrition values floored at 0 before scaling                        |
| M8  | Pure functions in LabelAnalysisScreen not extracted to `*-utils.ts` for testability                  | code-quality   | Architecture, Code Quality | `client/screens/LabelAnalysisScreen.tsx:61-156`        | verified | 3 functions extracted to `label-analysis-utils.ts`; 13 tests in new test file pass                |
| M9  | Missing unit tests for `useOCRDetection` hook (other hooks in same dir have tests)                   | code-quality   | Architecture, Code Quality | `client/camera/hooks/useOCRDetection.ts`               | verified | 10 tests in `useOCRDetection.test.ts` cover state transitions, debounce, haptics, latestOCRResult |
| M10 | Barcode lookup + verification are sequential where `Promise.all` possible                            | performance    | Performance                | `server/routes/nutrition.ts:98-105`                    | verified | `Promise.all([lookupBarcode, getVerification])` parallelized; grep confirms                       |
| M11 | RecipeBrowserScreen FlatList missing `FLATLIST_DEFAULTS` (renders combined large list)               | performance    | Performance                | `client/screens/meal-plan/RecipeBrowserScreen.tsx:741` | verified | `{...FLATLIST_DEFAULTS}` spread added; import confirmed                                           |
| M12 | `nutrientRows`/`macroRows`/`microRows` recomputed every render without `useMemo`                     | performance    | Performance                | `client/screens/LabelAnalysisScreen.tsx:314-316`       | verified | All 3 wrapped in `useMemo`; `useMemo` import added                                                |
| M13 | `useScanClassification` timeout cleanup captures refs at mount time (always null) — timeouts leak    | code-quality   | Code Quality               | `client/hooks/useScanClassification.ts:49-60`          | verified | Cleanup reads `.current` at cleanup time, not setup; eslint-disable for false-positive warning    |

### Low

| ID  | Finding                                                                                   | Domain         | Agent                             | File(s)                                                | Status         | Verification                                                                                                      |
| --- | ----------------------------------------------------------------------------------------- | -------------- | --------------------------------- | ------------------------------------------------------ | -------------- | ----------------------------------------------------------------------------------------------------------------- |
| L1  | Uncleared `setTimeout` in toast dismissal (state update on unmounted component)           | code-quality   | Camera, Performance, Code Quality | `client/screens/LabelAnalysisScreen.tsx:221`           | verified       | Timer tracked in `toastTimer` variable; cleared in cleanup function                                               |
| L2  | Aggressive `S→5` OCR correction — high false-positive rate vs other char fixes            | data-integrity | Security, Camera                  | `client/lib/nutrition-ocr-parser.ts:24`                | verified       | Narrowed to `(?<=\d)S\|S(?=\d)` — only replaces S adjacent to digits; new test passes                             |
| L3  | TODO comment + 6 `__DEV__` debug log blocks in new OCR code (frame-rate logging)          | code-quality   | Architecture, Code Quality        | `useOCRDetection.ts:70`, `ScanScreen`, `LabelAnalysis` | verified       | All 6 debug blocks removed from 3 files; `grep __DEV__ ScanScreen` returns 0                                      |
| L4  | Unused `LocalNutritionData` re-export from camera barrel (dead code)                      | architecture   | Architecture, Code Quality        | `client/camera/index.ts:5`                             | verified       | Re-export removed; `camera/index.ts` no longer references nutrition-ocr-parser                                    |
| L5  | CameraView renders `null` when `!isActive` instead of relying on Camera's `isActive` prop | performance    | Camera                            | `client/camera/components/CameraView.tsx:185-187`      | verified       | Early return removed; Camera stays mounted with `isActive={false}` for session reuse                              |
| L6  | `useOCRDetection` hook always instantiated even when OCR is disabled                      | performance    | Camera                            | `client/camera/components/CameraView.tsx:124-128`      | false-positive | React hooks rule prevents conditional calls; hook returns undefined frameProcessor when disabled; negligible cost |
| L7  | GET `/api/verification/:barcode` missing format validation (inconsistent with POST)       | security       | Security                          | `server/routes/verification.ts:563-566`                | verified       | `barcodeField.safeParse()` added; test updated to use numeric barcode; 24/24 tests pass                           |
| L8  | OCR parser has no upper-bound sanity checks (e.g., 25000 calories from OCR misread)       | data-integrity | Data Integrity                    | `client/lib/nutrition-ocr-parser.ts:28-31`             | verified       | `MAX_NUTRITION_VALUE = 10000` cap added; new test "rejects unreasonably large values" passes                      |
| L9  | `createMealSuggestionCache` missing `onConflictDoNothing` — concurrent inserts cause 500  | data-integrity | Data Integrity                    | `server/storage/cache.ts:173-183`                      | verified       | `.onConflictDoNothing()` added; falls back to `getMealSuggestionCache()` on conflict                              |
| L10 | Non-null assertion `sessionId!` in mutation (should use runtime guard)                    | code-quality   | Code Quality                      | `client/screens/LabelAnalysisScreen.tsx:257`           | verified       | Replaced with `if (!sessionId) throw new Error("No session")`; 0 non-null assertions remain                       |
| L11 | Missing parser test coverage for `S→5` path and negative/garbage values                   | code-quality   | Code Quality                      | `client/lib/__tests__/nutrition-ocr-parser.test.ts`    | verified       | 6 new test cases added: S→5, negatives, upper-bound, garbage, "Calories from Fat", servingSize cap                |
| L12 | Unbounded `servingSize` string from OCR parser (no length cap on `.+` regex)              | security       | Security                          | `client/lib/nutrition-ocr-parser.ts:79-82`             | verified       | `.slice(0, 100)` added; new test "caps serving size string length" passes                                         |
| L13 | 3 FlatLists missing `FLATLIST_DEFAULTS` (MenuScanResult, BatchSummary, CookSessionReview) | performance    | Performance                       | 3 screen files                                         | verified       | `{...FLATLIST_DEFAULTS}` spread added to all 3; imports confirmed                                                 |

### False-positives

| ID  | Finding                                                 | Agent        | Reason                                                                         |
| --- | ------------------------------------------------------- | ------------ | ------------------------------------------------------------------------------ |
| FP1 | Public API wildcard CORS                                | Security     | Safe for API-key-auth endpoint; credentials not enabled                        |
| FP2 | LabelAnalysis missing deep linking config               | Architecture | Screen requires `imageUri` runtime param; no deep link use case                |
| FP3 | daily-summary endpoint sequential DB call               | Performance  | `getPlannedNutritionSummary` depends on `confirmedIds` — sequential is correct |
| FP4 | `useOCRDetection` always instantiated (L6 reclassified) | Camera       | React hooks rule prevents conditional calls; negligible cost when disabled     |

## Deferred Items

_None — all findings fixed._

## Summary

| Severity  | Found | Verified | Deferred | False-positive | Open  |
| --------- | ----- | -------- | -------- | -------------- | ----- |
| Critical  | 0     | 0        | 0        | 0              | 0     |
| High      | 2     | 2        | 0        | 0              | 0     |
| Medium    | 13    | 13       | 0        | 0              | 0     |
| Low       | 13    | 12       | 0        | 1              | 0     |
| **Total** | 28    | 27       | 0        | 4              | **0** |

## Fix Commits

| Commit | Description |
| ------ | ----------- |
| —      | (pending)   |

## Codification (Phase 7)

Completed after fixes are committed. Each row links to the docs change.

### Patterns Extracted

| Finding     | Pattern                                                    | Added To                    |
| ----------- | ---------------------------------------------------------- | --------------------------- |
| M5/M7/M6/L8 | Defense-in-depth: Client-to-DB numeric validation pipeline | `docs/patterns/security.md` |
| M6          | Non-negative CHECK constraints on all nutrition tables     | `docs/patterns/database.md` |

### Learnings Extracted

| Finding | Learning Title                                            | Category       |
| ------- | --------------------------------------------------------- | -------------- |
| M13     | React effect cleanup must read timer refs at cleanup time | Gotcha         |
| M2      | OCR regex must account for prefix lines sharing keywords  | Data Integrity |
| L2      | OCR character corrections must be context-sensitive       | Data Integrity |

### Code Reviewer Updates

| Finding     | New Check Added                                 |
| ----------- | ----------------------------------------------- |
| M5/M7/M6/L8 | Client-to-DB numeric validation pipeline        |
| M6          | Nutrition table CHECK constraints               |
| M1          | cancelAnimation before static value assignment  |
| M13         | Timer refs read at cleanup time, not setup time |

### Specialist Agent Updates

| Finding | Agent Updated            | New Check Added                                              |
| ------- | ------------------------ | ------------------------------------------------------------ |
| M1      | `camera-specialist.md`   | cancelAnimation in reducedMotion branches                    |
| M13     | `camera-specialist.md`   | Timer ref cleanup timing                                     |
| M2      | `camera-specialist.md`   | OCR regex keyword collision (Common Mistakes #7)             |
| L2      | `camera-specialist.md`   | Context-sensitive OCR corrections (Common Mistakes #8)       |
| M6      | `database-specialist.md` | Nutrition table CHECK constraints (Schema Changes checklist) |
| L9      | `database-specialist.md` | onConflictDoNothing for cache inserts (Common Mistakes #12)  |

**Codification commit:** (pending)

## Post-Audit Notes

- 7th full audit. New OCR feature code was the primary focus (12 commits, ~2200 LOC).
- Camera agent found the most critical OCR-specific issues (calories regex, withRepeat animation leak).
- Data integrity agent caught the barcodeNutrition CHECK gap and OCR parser validation holes.
- Performance agent identified mutation dep instability (H1) — a subtle TanStack Query v5 pattern violation.
- M13 (useScanClassification timeout cleanup) was a pre-existing bug caught during the general sweep.
- L6 reclassified as false-positive: React hooks rule prevents conditional hook calls.
- +28 tests added (6 parser edge cases, 13 label-analysis-utils, 10 useOCRDetection hook).
