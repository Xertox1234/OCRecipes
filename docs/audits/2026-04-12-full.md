# Audit: Full Codebase Audit

> **Date:** 2026-04-12
> **Trigger:** Periodic full audit — ~45 commits, ~16K LOC since audit #9 (Coach Pro, serving adjuster, UI interactions, favourites deferred items, accessibility)
> **Domains:** security, performance, data-integrity, architecture, code-quality, camera
> **Baseline:** 3534 tests passing (251 files), 1 failed (suggestions timeout) | 0 type errors | 0 lint errors (2 warnings)
> **Raw agent findings:** 41 across 6 domains (5 security, 10 performance, 8 data-integrity, 6 architecture, 12 code-quality, 0 camera) → 29 after dedup
> **Post-fix:** 3535 tests passing (251 files) | 0 type errors | 0 lint errors (2 warnings)

## Findings

Each finding has a lifecycle: `open` → `fixing` → `verified` or `deferred` or `false-positive`.

**Status key:**

- `open` — Found but not yet addressed
- `fixing` — Work in progress
- `verified` — Fix applied AND confirmed by test/grep/type-check
- `deferred` — Intentionally postponed (must link to todo)
- `false-positive` — Agent was wrong or issue was already fixed

### Critical

| ID  | Finding                                                                                                                                                                                             | Domain         | Agent          | File(s)                         | Status   | Verification                                                                                    |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------------- | ------------------------------- | -------- | ----------------------------------------------------------------------------------------------- |
| C1  | Coach response cache cross-user data leakage — `coachResponseCache` key is SHA-256 of question text only, no `userId`; User A's personalized nutrition advice served to User B asking same question | data-integrity | Data Integrity | `server/routes/chat.ts:523-588` | verified | `userId:` prefix added to hash input; grep confirms `req.userId` in hash; 28/28 chat tests pass |

### High

| ID  | Finding                                                                                                                                                                                                                       | Domain                      | Agent                       | File(s)                                                                   | Status   | Verification                            |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | --------------------------- | ------------------------------------------------------------------------- | -------- | --------------------------------------- |
| H1  | Unbounded notebook growth — `getActiveNotebookEntries` has no LIMIT clause; `archiveOldEntries` exists but never called from any route/service/cron; entries accumulate permanently per user (up to 10 per conversation turn) | performance, data-integrity | Performance, Data Integrity | `server/storage/coach-notebook.ts:13-29,75-94`                            | deferred | `todos/notebook-lifecycle.md`           |
| H2  | Cross-route import — `consumeWarmUp` exported from `coach-context.ts` and imported by `chat.ts`; in-memory stateful warm-up cache (Map + setInterval sweep) lives in a route file instead of a service/storage module         | architecture                | Architecture                | `server/routes/coach-context.ts:9-32,171-183`, `server/routes/chat.ts:37` | deferred | `todos/coach-pro-service-extraction.md` |
| H3  | Missing test coverage — `coach-context.ts` route (GET + POST) has zero route-level tests; only route module without a test file                                                                                               | code-quality                | Code Quality                | `server/routes/coach-context.ts`                                          | deferred | `todos/coach-pro-test-coverage.md`      |
| H4  | Missing test coverage — `generateCoachProResponse` tool-calling loop (165 LOC, most complex new logic) has no unit tests; only tested indirectly via chat route tests with mocked behavior                                    | code-quality                | Code Quality                | `server/services/nutrition-coach.ts:172-336`                              | deferred | `todos/coach-pro-test-coverage.md`      |

### Medium

| ID  | Finding                                                                                                                                                                                                                                                                                      | Domain         | Agent                                | File(s)                                                                              | Status   | Verification                                                                                                                         |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------ | ------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| M1  | Missing rate limiter on coach-context endpoints — both GET `/api/coach/context` (fires 4 parallel DB queries) and POST `/api/coach/warm-up` lack rate limiting; only route module without it                                                                                                 | security       | Security, Architecture, Code Quality | `server/routes/coach-context.ts:36-38,102-105`                                       | verified | `crudRateLimit` added to both endpoints; grep confirms 3 occurrences in file; 28/28 chat tests pass                                  |
| M2  | Unvalidated navigation params from AI blocks — `navigateActionSchema.params` is `z.record(z.unknown())`, screen name whitelist validates target but params shape unchecked; AI could supply arbitrary itemId/recipeId                                                                        | security       | Security, Data Integrity             | `shared/schemas/coach-blocks.ts:35`, `client/components/coach/CoachChat.tsx:268-332` | deferred | `todos/coach-pro-hardening.md`                                                                                                       |
| M3  | Sequential tool call execution — independent tool calls (`lookup_nutrition`, `search_recipes`, etc.) run in `for...of` loop; could use `Promise.allSettled` to save hundreds of ms per multi-tool response                                                                                   | performance    | Performance                          | `server/services/nutrition-coach.ts:306-327`                                         | deferred | `todos/coach-pro-service-extraction.md`                                                                                              |
| M4  | Streaming hot path: regex with backtracking (`[\s\S]*?`) applied against growing accumulated string on every SSE chunk + duplicate `scrollToEnd` (once in onChunk callback, once in useEffect)                                                                                               | performance    | Performance                          | `client/components/coach/CoachChat.tsx:228-233,357-360`                              | deferred | `todos/coach-pro-streaming-perf.md`                                                                                                  |
| M5  | Zod validation (`filterValidBlocks` → `coachBlockSchema.safeParse`) runs in render path inside `messages.map()` IIFE on every streaming tick; blocks should be validated once during hydration                                                                                               | performance    | Performance                          | `client/components/coach/CoachChat.tsx:383-401`                                      | deferred | `todos/coach-pro-streaming-perf.md`                                                                                                  |
| M6  | SSE response size guard (`SSE_MAX_RESPONSE_BYTES = 50KB`) enforced on recipe/remix path (line 385) but missing from coach and coach-pro streaming loops — unbounded SSE response                                                                                                             | code-quality   | Code Quality                         | `server/routes/chat.ts:559-577`                                                      | deferred | `todos/coach-pro-hardening.md`                                                                                                       |
| M7  | Client SSE error events not handled — server sends `{error: "Response timeout"}` and `{error: "Failed to generate response"}` but client only checks `data.content`/`data.blocks`/`data.done`; `isStreaming` stuck true permanently                                                          | code-quality   | Code Quality                         | `client/components/coach/CoachChat.tsx:96-113`                                       | verified | `data.error` check added before content handling, calls `onDone()` to reset streaming state; 9/9 coach-chat-utils tests pass         |
| M8  | `add_to_meal_plan` tool: handler references `args.recipeId` (line 413) but schema only defines `plannedDate`, `mealType`, `notes` — recipeId always defaults to 0; `notes` param unused in handler                                                                                           | code-quality   | Code Quality                         | `server/services/coach-tools.ts:196-222,408-420`                                     | verified | Phantom `recipeId` removed; `notes` from args now included in proposal response; grep confirms no recipeId in file; 36/36 tests pass |
| M9  | `followUpDate` not validated as date format — Zod schema uses `z.string().nullable().optional()` instead of `z.string().date()`; invalid AI output causes `new Date("next Tuesday")` → Invalid Date → PostgreSQL error → entire `createNotebookEntries` batch fails silently (fireAndForget) | data-integrity | Data Integrity, Code Quality         | `shared/schemas/coach-notebook.ts:28`, `server/routes/chat.ts:647`                   | verified | Regex `/^\d{4}-\d{2}-\d{2}/` added to validate ISO date format; 52/52 schema+chat+notebook tests pass                                |
| M10 | Duplicate user message in notebook extraction — user message saved to DB, then `getChatMessages` returns it in history, then it's appended again in `allMessages` array; AI extraction sees message twice                                                                                    | data-integrity | Data Integrity                       | `server/routes/chat.ts:282,444,502,629-632`                                          | deferred | `todos/coach-pro-hardening.md`                                                                                                       |
| M11 | 470-line handler `POST /api/chat/conversations/:id/messages` — orchestrates 3+ storage domains, 3 code paths (recipe/remix, coach, coach-pro), SSE lifecycle, caching, notebook extraction inline                                                                                            | architecture   | Architecture                         | `server/routes/chat.ts:210-680`                                                      | deferred | `todos/coach-pro-service-extraction.md`                                                                                              |

### Low

| ID  | Finding                                                                                                                                                                                     | Domain                                                | Agent                        | File(s)                                                                                                         | Status                                      | Verification                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ----------------------------------- | ------------------------------ |
| L1  | Tool call error messages may leak internal details (DB connection strings, table names) to AI context → AI could surface them in response                                                   | security                                              | Security                     | `server/services/nutrition-coach.ts:312-319`                                                                    | deferred                                    | `todos/coach-pro-hardening.md`      |
| L2  | No defense-in-depth length constraint on notebook content in storage — `createNotebookEntries` accepts `InsertCoachNotebookEntry[]` directly; DB column is `text` with no length constraint | security                                              | Security                     | `server/storage/coach-notebook.ts:38-43`                                                                        | deferred                                    | `todos/coach-pro-hardening.md`      |
| L3  | `shouldUpdateStrategy(0)` returns true — coaching strategy extracted on first-ever conversation before any signal exists; wastes AI tokens, produces low-signal entry                       | data-integrity                                        | Security, Data Integrity     | `server/services/notebook-extraction.ts:98`                                                                     | deferred                                    | `todos/coach-pro-hardening.md`      |
| L4  | Hardcoded protein goal (150g) in coach context suggestions — ignores user's actual `dailyProteinGoal` from `users` table                                                                    | data-integrity                                        | Data Integrity, Architecture | `server/routes/coach-context.ts:63`                                                                             | deferred                                    | `todos/coach-pro-hardening.md`      |
| L5  | Coach context returns raw allergy objects `{name, severity}` but `CoachContext.dietaryProfile.allergies` expects `string[]` — format inconsistency between GET endpoint and chat route      | data-integrity                                        | Data Integrity               | `server/routes/coach-context.ts:87`, `server/routes/chat.ts:470-472`                                            | deferred                                    | `todos/coach-pro-hardening.md`      |
| L6  | XHR `responseText` memory accumulation — full SSE payload held in memory twice (xhr buffer + `accumulated` variable) during Coach Pro streaming                                             | performance                                           | Performance                  | `client/components/coach/CoachChat.tsx:80-128`                                                                  | deferred                                    | `todos/coach-pro-streaming-perf.md` |
| L7  | Suboptimal index for `getCommitmentsWithDueFollowUp` — `(userId, type, status)` index can't range-scan on `followUpDate`; ideal would be `(userId, type, status, followUpDate)`             | performance                                           | Performance                  | `server/storage/coach-notebook.ts:58-73`                                                                        | deferred                                    | `todos/notebook-lifecycle.md`       |
| L8  | `CoachDashboard` not memoized — re-renders on every CoachChat streaming state change via parent; inline `.filter().slice().map()` on notebook array runs without useMemo                    | performance                                           | Performance                  | `client/components/coach/CoachDashboard.tsx`, `client/screens/CoachProScreen.tsx:78-83`                         | deferred                                    | `todos/coach-pro-streaming-perf.md` |
| L9  | `MealPlanDay` type imported from coach-blocks schema into core navigation params — inverted dependency, plan-tab feature depends on Coach Pro schema                                        | architecture                                          | Architecture                 | `client/navigation/RootStackNavigator.tsx:37,135`                                                               | deferred                                    | `todos/coach-pro-type-cleanup.md`   |
| L10 | `CoachChatNavigationProp` defined locally in CoachChat instead of centralized `client/types/navigation.ts`                                                                                  | architecture                                          | Architecture                 | `client/components/coach/CoachChat.tsx:44-50`                                                                   | deferred                                    | `todos/coach-pro-type-cleanup.md`   |
| L11 | Hex suffix color opacity — 3 instances use `theme.link + "33"` / `"26"` / `"4D"` instead of `withOpacity()` utility                                                                         | code-quality                                          | Code Quality                 | `client/components/coach/blocks/CommitmentCard.tsx:59`, `client/components/coach/blocks/QuickReplies.tsx:26-27` | deferred                                    | `todos/coach-pro-type-cleanup.md`   |
| L12 | Snake_case variable name `parsed_blocks` in camelCase codebase                                                                                                                              | code-quality                                          | Code Quality                 | `server/routes/chat.ts:595`                                                                                     | deferred                                    | `todos/coach-pro-type-cleanup.md`   |
| L13 | `notebook-extraction.ts` role type assertion casts all roles to `"user"                                                                                                                     | "assistant"`— silently drops`"system"` role if passed | code-quality                 | Code Quality                                                                                                    | `server/services/notebook-extraction.ts:63` | deferred                            | `todos/coach-pro-hardening.md` |

### False-positives

| ID  | Finding                             | Agent        | Reason                                                              |
| --- | ----------------------------------- | ------------ | ------------------------------------------------------------------- |
| —   | Camera: no new findings             | Camera       | No camera code changed since last audit                             |
| —   | Suggestions test timeout (line 235) | Code Quality | Not reproducible — all 18 tests pass in 28ms; intermittent CI issue |

## Deferred Items

Items marked `deferred` must have a linked todo and rationale.

| ID                                   | Todo                                    | Rationale                                                                                               |
| ------------------------------------ | --------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| H1, L7                               | `todos/notebook-lifecycle.md`           | Requires LIMIT strategy decision + archival trigger design; no immediate data loss risk                 |
| H2, M3, M11                          | `todos/coach-pro-service-extraction.md` | Architectural refactor — extract service, parallelize tools, decompose handler; needs dedicated session |
| H3, H4                               | `todos/coach-pro-test-coverage.md`      | Dedicated test-writing session for route + service; no production behavior affected                     |
| M2, M6, M10, L1, L2, L3, L4, L5, L13 | `todos/coach-pro-hardening.md`          | Mixed data quality and defense-in-depth items; none causing active bugs, bundled for efficiency         |
| M4, M5, L6, L8                       | `todos/coach-pro-streaming-perf.md`     | Performance optimizations for streaming hot path; requires profiling to prioritize                      |
| L9, L10, L11, L12                    | `todos/coach-pro-type-cleanup.md`       | Mechanical type/style fixes — no logic changes                                                          |

## Summary

| Severity  | Found | Verified | Deferred | False-positive | Open  |
| --------- | ----- | -------- | -------- | -------------- | ----- |
| Critical  | 1     | 1        | 0        | 0              | 0     |
| High      | 4     | 0        | 4        | 0              | 0     |
| Medium    | 11    | 4        | 7        | 0              | 0     |
| Low       | 13    | 0        | 13       | 0              | 0     |
| **Total** | 29    | 5        | 24       | 0              | **0** |

## Fix Commits

| Commit    | Description                                                |
| --------- | ---------------------------------------------------------- |
| `6a999d1` | fix: resolve full audit findings (5 verified, 24 deferred) |

## Codification (Phase 7)

Completed after fixes are committed. Each row links to the docs change.

### Patterns Extracted

| Finding | Pattern                                                                                        | Added To                    |
| ------- | ---------------------------------------------------------------------------------------------- | --------------------------- |
| C1      | Cache key must include all dimensions that vary the response (userId for personalized content) | `docs/patterns/database.md` |

### Learnings Extracted

| Finding | Learning Title                                        | Category                |
| ------- | ----------------------------------------------------- | ----------------------- |
| M7      | Fix One Protocol Handler, Grep All Consumers          | Gotcha (Code Review)    |
| M8      | OpenAI Tool Schema/Handler Drift — Phantom Parameters | Gotcha (AI Integration) |

### Code Reviewer Updates

| Finding | New Check Added |
| ------- | --------------- |
| —       | —               |

### Specialist Agent Updates

| Finding | Agent Updated                         | New Check Added                                                               |
| ------- | ------------------------------------- | ----------------------------------------------------------------------------- |
| M8      | `.claude/agents/ai-llm-specialist.md` | Tool schema/handler drift — args must match schema properties bidirectionally |

**Codification commit:** `8440172`

## Post-Audit Notes

- 10th full audit. Coach Pro was the primary source of findings (27 of 29) — expected for a large new feature (~16K LOC).
- Critical finding was cross-user cache leakage: `coachResponseCache` key had no `userId` dimension, so personalized nutrition advice could be served to wrong users.
- Camera domain clean again — no code changed since audit #8.
- Architecture agent identified the biggest structural concern: `chat.ts` POST handler at 470 lines with cross-route import for warm-up cache.
- All 6 agents contributed unique findings. Data Integrity found the only Critical. Security's IDOR review came back clean — Coach Pro ownership checks are solid.
- 24 items deferred to 6 todos, organized by scope: service extraction, test coverage, notebook lifecycle, streaming perf, hardening, type cleanup.
